# VS Code Design System (设计系统)

> 本文档提取自 VS Code 开源项目，帮助你在自己的项目中复用 VS Code 的前端风格。

---

## 目录

1. [全局设计系统 (Global Design System)](#1-全局设计系统-global-design-system)
   - [设计原则](#11-设计原则)
   - [全局设计规范](#12-全局设计规范)
   - [颜色系统概览](#13-颜色系统概览)
   - [通用样式变量](#14-通用样式变量)
2. [基础组件 (Base Components)](#2-基础组件-base-components)
   - [Button 按钮](#21-button-按钮)
   - [IconLabel 图标标签](#22-iconlabel-图标标签)
   - [KeybindingLabel 快捷键标签](#23-keybindinglabel-快捷键标签)
   - [HighlightedLabel 高亮标签](#24-highlightedlabel-高亮标签)
   - [CountBadge 计数徽章](#25-countbadge-计数徽章)
   - [Separator & Sash 分割线与拖拽条](#26-separator--sash-分割线与拖拽条)
3. [表单与输入 (Form & Input)](#3-表单与输入-form--input)
   - [InputBox 输入框](#31-inputbox-输入框)
   - [FindInput 查找输入框](#32-findinput-查找输入框)
   - [SelectBox 下拉选择框](#33-selectbox-下拉选择框)
   - [Dropdown 下拉菜单](#34-dropdown-下拉菜单)
   - [Radio 单选按钮组](#35-radio-单选按钮组)
   - [Toggle & Checkbox 开关与复选框](#36-toggle--checkbox-开关与复选框)
4. [导航与操作 (Navigation & Actions)](#4-导航与操作-navigation--actions)
   - [Menu 菜单](#41-menu-菜单)
   - [Toolbar 工具栏](#42-toolbar-工具栏)
   - [Breadcrumbs 面包屑导航](#43-breadcrumbs-面包屑导航)
5. [数据展示与布局 (Data Display & Layout)](#5-数据展示与布局-data-display--layout)
   - [List 列表](#51-list-列表)
   - [Tree 树形控件](#52-tree-树形控件)
   - [Table 表格](#53-table-表格)
   - [SplitView 分割视图](#54-splitview-分割视图)
   - [Grid 网格布局](#55-grid-网格布局)
   - [Scrollbar 滚动条](#56-scrollbar-滚动条)
6. [反馈与浮层 (Feedback & Overlays)](#6-反馈与浮层-feedback--overlays)
   - [Dialog 对话框](#61-dialog-对话框)
   - [Hover 悬停提示](#62-hover-悬停提示)
   - [ProgressBar 进度条](#63-progressbar-进度条)
7. [附录：颜色注册表 (Appendix)](#7-附录颜色注册表-appendix)
   - [完整颜色系统参考](#71-完整颜色系统参考)
8. [Web 项目适配指南 (Web Adaptation Guide)](#8-web-项目适配指南-web-adaptation-guide)
   - [基础环境配置](#81-基础环境配置)
   - [兼容性处理](#82-兼容性处理)
   - [常见问题修复](#83-常见问题修复)

---

## 1. 全局设计系统 (Global Design System)

### 1.1 设计原则

#### 视觉风格
- **圆角**: 统一使用 `border-radius: 2px` (小组件) 或 `3px` (大组件如对话框)
- **字体**: 优先使用 MiSans，回退到系统字体 `font-family: "MiSans", inherit`
- **行高**: 标准行高 `line-height: 18px` 或 `22px`
- **间距**: 内边距通常为 `4px 6px` 或 `4px 10px`

#### 无障碍
- 所有交互元素必须有 `aria-label`
- 焦点边框使用 `focusBorder` 颜色
- 高对比度主题颜色对比度 ≥ 4.5:1

### 1.2 全局设计规范

本节涵盖 VS Code 的全局设计系统，包括字体、间距、动画、阴影、圆角和 Z-Index 层级规范。

#### 1.2.1 字体系统

VS Code 使用分离的字体策略：**UI 字体**用于界面元素，**编辑器字体**用于代码编辑。

**UI 字体 (DEFAULT_FONT_FAMILY)**
界面元素的系统字体，推荐使用以下 CSS 字体栈以确保跨平台一致性（优先使用 MiSans）：

```css
:root {
  /* 推荐的跨平台字体栈 */
  --vscode-font-family: "MiSans", -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", "Oxygen", "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif;
}

body {
  font-family: var(--vscode-font-family);
}
```

**编辑器字体 (EDITOR_FONT_DEFAULTS)**
代码编辑区使用等宽字体：
```typescript
// 来源: src/vs/editor/common/config/fontInfo.ts
const EDITOR_FONT_DEFAULTS = {
  fontFamily: {
    windows: 'Consolas, "Courier New", monospace',
    macOS: 'Menlo, Monaco, "Courier New", monospace',
    linux: '"Droid Sans Mono", "monospace", monospace'
  },
  fontWeight: 'normal', // macOS Standard: 400
  fontSize: {
    macOS: 12,
    other: 14
  },
  lineHeight: 0,        // 0 表示自动计算
  letterSpacing: 0
};
```

**字重规范 (Font Weights)**
遵循 macOS 标准字重：
- **Regular**: 400 (正常文本)
- **Medium**: 500 (强调/标题)
- **Bold**: 700 (强强调)

**字体规范速查表**

| 场景 | 字体系列 | 字号 | 行高 |
|------|---------|------|------|
| UI 界面 | MiSans, 系统 UI 字体 | 13px (默认) | 继承 |
| 编辑器 (macOS) | Menlo, Monaco | 12px | 18px |
| 编辑器 (Windows) | Consolas | 14px | 19px |
| 编辑器 (Linux) | Droid Sans Mono | 14px | 19px |
| 图标 (Codicon) | codicon | 16px | 1 |
| 徽章文字 | 继承 | 11px | 16px |
| 快捷键标签 | 继承 | 11px | - |

#### 1.2.2 间距系统

VS Code 使用一致的间距系统确保视觉协调。

**基础间距单位**
```css
/* VS Code 常用间距值 */
--spacing-xxs: 2px;   /* 极小 */
--spacing-xs: 3px;    /* 超小 */
--spacing-sm: 4px;    /* 小 */
--spacing-md: 6px;    /* 中等 */
--spacing-lg: 8px;    /* 大 */
--spacing-xl: 10px;   /* 超大 */
--spacing-xxl: 16px;  /* 极大 */
```

**组件内边距 (Padding) 规范**

| 组件 | 内边距 | 说明 |
|------|--------|------|
| **ActionBar 项目** | `3px` | 工具栏按钮 |
| **Badge 标准** | `3px 5px` | 徽章标准尺寸 |
| **Badge 紧凑** | `2px 3px` | 紧凑型徽章 |
| **Toggle** | `1px` | 开关按钮 |
| **Tree 筛选器** | `3px` | 树视图筛选器 |
| **Tree 筛选器输入** | `2px 4px` | 输入框内边距 |
| **Tree 高亮** | `2px` | 高亮匹配项 |
| **Menu 项目** | 垂直可变 | 菜单项 |

#### 1.2.3 动画与过渡

VS Code 使用微妙的动画增强用户体验，同时保持轻量级。

**标准过渡时长**
```css
/* 过渡时长规范 */
--transition-fastest: 70ms;     /* 极快 - 微交互 */
--transition-fast: 100ms;       /* 快速 - 状态变化 */
--transition-normal: 150ms;     /* 标准 - 通用过渡 */
--transition-medium: 200ms;     /* 中等 - 面板移动 */
--transition-slow: 300ms;       /* 慢速 - 大面积变化 */
--transition-slower: 500ms;     /* 较慢 - 复杂动画 */
```

**动画规范速查表**

| 动画类型 | 时长 | 缓动 | 用途 |
|----------|------|------|------|
| 微交互 | 70-100ms | ease-out | 状态变化、悬停 |
| 面板过渡 | 200ms | linear | 查找面板、滑动 |
| 透明度 | 100-300ms | linear/ease | 显示/隐藏 |
| 加载旋转 | 1.25-1.5s | steps(30) | 进度指示 |
| 光标闪烁 | 1s | step-end | 编辑器光标 |
| 脉冲效果 | 2-2.5s | ease-in-out | AI 处理、注意力 |

#### 1.2.4 阴影系统

VS Code 使用轻量级阴影增强层级感。

**阴影规范速查表**

| 类型 | 阴影值 | 用途 |
|------|--------|------|
| 标准 Widget | `0 2px 8px shadow` | 悬停、对话框、菜单 |
| 查找 Widget | `0 0 8px 2px shadow` | 查找/替换面板 |
| 滚动指示 | `0 6px 6px -6px inset` | 内容溢出指示 |
| 焦点边框 | `inset 0 0 0 1px border` | 焦点状态 |
| 无阴影 | `none` / `unset` | 高对比度主题 |

#### 1.2.5 圆角系统

VS Code 使用保守的圆角设计，保持专业和轻量感。

**圆角规范**
```css
/* 圆角尺寸规范 */
--radius-none: 0;
--radius-xs: 2px;     /* 极小 - 徽章、标签 */
--radius-sm: 3px;     /* 小 - 代码块、建议列表 */
--radius-md: 4px;     /* 中等 - 筛选器、输入框 */
--radius-lg: 5px;     /* 大 - 面板、卡片 */
--radius-xl: 6px;     /* 超大 - Chat 区域 */
--radius-pill: 10px;  /* 药丸形 - 头像、状态 */
--radius-circle: 50%; /* 圆形 - 加载指示器 */
--radius-full: 100%;  /* 完全圆形 */
```

#### 1.2.6 Z-Index 层级系统

VS Code 使用分层的 z-index 系统确保正确的堆叠顺序。

**层级设计原则**
```
┌─────────────────────────────────────┐
│          最高层 (10000+)            │  ← 全局拖拽操作
├─────────────────────────────────────┤
│          弹出层 (1000-3000)         │  ← 对话框、菜单、悬停
├─────────────────────────────────────┤
│          覆盖层 (50-100)            │  ← 激活的交互元素
├─────────────────────────────────────┤
│          内容层 (10-50)             │  ← 滚动条、焦点指示
├─────────────────────────────────────┤
│          基础层 (0-10)              │  ← 内容、分割线
└─────────────────────────────────────┘
```

#### 1.2.7 无障碍规范

VS Code 遵循 WCAG 2.1 指南，确保所有用户都能使用。

**无障碍规范速查表**

| 要求 | 规范 | 实现方式 |
|------|------|----------|
| **键盘可访问** | 所有功能可通过键盘操作 | tabindex、快捷键 |
| **焦点可见** | 焦点状态必须清晰可见 | outline、border |
| **颜色对比度** | 至少 4.5:1 (正常) / 3:1 (大文本) | 颜色变量 |
| **屏幕阅读器** | 正确的 ARIA 标签 | role、aria-* |
| **减少动画** | 尊重用户偏好 | prefers-reduced-motion |
| **高对比度** | 专用主题 + 增强边框 | hcDark/hcLight 主题 |
| **文本缩放** | 支持 200% 缩放 | 相对单位、响应式 |

#### 1.2.8 CSS 变量命名规范

**命名模式**
```css
/* 基础模式 */
--vscode-{category}-{element}-{property}-{state}

/* 示例 */
--vscode-editor-background           /* 编辑器背景 */
--vscode-button-foreground           /* 按钮前景 */
--vscode-input-border                /* 输入框边框 */
--vscode-list-activeSelectionBackground  /* 列表激活选中背景 */
--vscode-editorError-foreground      /* 编辑器错误前景 */
```

### 1.3 颜色系统概览

VS Code 使用 CSS 变量定义颜色，支持四种主题模式：

| 模式 | 说明 |
|------|------|
| `dark` | 深色主题 |
| `light` | 浅色主题 |
| `hcDark` | 高对比度深色 |
| `hcLight` | 高对比度浅色 |

#### 基础颜色

```css
:root {
  /* 前景色 */
  --vscode-foreground: #CCCCCC; /* dark: #CCCCCC, light: #616161 */
  --vscode-disabledForeground: #CCCCCC80;
  --vscode-errorForeground: #F48771; /* dark: #F48771, light: #A1260D */
  --vscode-descriptionForeground: rgba(204, 204, 204, 0.7);
  --vscode-icon-foreground: #C5C5C5;
  
  /* 焦点边框 */
  --vscode-focusBorder: #007FD4; /* dark: #007FD4, light: #0090F1 */
  
  /* 链接 */
  --vscode-textLink-foreground: #3794FF; /* dark: #3794FF, light: #006AB1 */
}
```

#### 按钮颜色

```css
:root {
  /* 主按钮 */
  --vscode-button-background: #0E639C; /* dark: #0E639C, light: #007ACC */
  --vscode-button-hoverBackground: /* 自动加深/变浅 20% */
  --vscode-button-foreground: #FFFFFF;
  --vscode-button-border: transparent;
  --vscode-button-separator: rgba(255, 255, 255, 0.4);
  
  /* 次要按钮 */
  --vscode-button-secondaryBackground: #3A3D41; /* dark: #3A3D41, light: #5F6A79 */
  --vscode-button-secondaryForeground: #FFFFFF;
  --vscode-button-secondaryHoverBackground: /* 自动加深/变浅 20% */
}
```

#### 输入框颜色

```css
:root {
  /* 输入框 */
  --vscode-input-background: #3C3C3C; /* dark: #3C3C3C, light: #FFFFFF */
  --vscode-input-foreground: #CCCCCC;
  --vscode-input-border: transparent; /* 高对比度模式有边框 */
  --vscode-input-placeholderForeground: rgba(204, 204, 204, 0.5);
  
  /* 输入选项（如搜索框中的正则按钮） */
  --vscode-inputOption-activeBorder: #007ACC;
  --vscode-inputOption-activeBackground: rgba(0, 127, 212, 0.4);
  --vscode-inputOption-activeForeground: #FFFFFF;
  --vscode-inputOption-hoverBackground: rgba(90, 93, 94, 0.5);
  
  /* 验证状态 */
  --vscode-inputValidation-infoBackground: #063B49;
  --vscode-inputValidation-infoBorder: #007acc;
  --vscode-inputValidation-warningBackground: #352A05;
  --vscode-inputValidation-warningBorder: #B89500;
  --vscode-inputValidation-errorBackground: #5A1D1D;
  --vscode-inputValidation-errorBorder: #BE1100;
}
```

#### 下拉框颜色

```css
:root {
  --vscode-dropdown-background: #3C3C3C; /* dark: #3C3C3C, light: #FFFFFF */
  --vscode-dropdown-foreground: #F0F0F0;
  --vscode-dropdown-border: #3C3C3C; /* light: #CECECE */
  --vscode-dropdown-listBackground: /* 与 background 相同或 null */
}
```

#### 复选框颜色

```css
:root {
  --vscode-checkbox-background: #3C3C3C;
  --vscode-checkbox-foreground: #F0F0F0;
  --vscode-checkbox-border: #3C3C3C;
}
```

### 1.4 通用样式变量

#### 完整 CSS 变量参考

```css
:root {
  /* ===== 基础 ===== */
  --vscode-foreground: #CCCCCC;
  --vscode-disabledForeground: #CCCCCC80;
  --vscode-errorForeground: #F48771;
  --vscode-descriptionForeground: rgba(204, 204, 204, 0.7);
  --vscode-icon-foreground: #C5C5C5;
  --vscode-focusBorder: #007FD4;
  --vscode-contrastBorder: transparent;
  --vscode-selection-background: /* 自定义 */;
  
  /* ===== 链接 ===== */
  --vscode-textLink-foreground: #3794FF;
  --vscode-textLink-activeForeground: #3794FF;
  --vscode-textSeparator-foreground: rgba(255, 255, 255, 0.18);
  
  /* ===== 按钮 ===== */
  --vscode-button-background: #0E639C;
  --vscode-button-hoverBackground: #1177BB;
  --vscode-button-foreground: #FFFFFF;
  --vscode-button-border: transparent;
  --vscode-button-separator: rgba(255, 255, 255, 0.4);
  --vscode-button-secondaryBackground: #3A3D41;
  --vscode-button-secondaryHoverBackground: #4a4d51;
  --vscode-button-secondaryForeground: #FFFFFF;
  
  /* ===== 输入框 ===== */
  --vscode-input-background: #3C3C3C;
  --vscode-input-foreground: #CCCCCC;
  --vscode-input-border: transparent;
  --vscode-input-placeholderForeground: rgba(204, 204, 204, 0.5);
  --vscode-inputOption-activeBorder: #007ACC;
  --vscode-inputOption-activeBackground: rgba(0, 127, 212, 0.4);
  --vscode-inputOption-activeForeground: #FFFFFF;
  --vscode-inputOption-hoverBackground: rgba(90, 93, 94, 0.5);
  
  /* ===== 输入验证 ===== */
  --vscode-inputValidation-infoBackground: #063B49;
  --vscode-inputValidation-infoBorder: #007acc;
  --vscode-inputValidation-warningBackground: #352A05;
  --vscode-inputValidation-warningBorder: #B89500;
  --vscode-inputValidation-errorBackground: #5A1D1D;
  --vscode-inputValidation-errorBorder: #BE1100;
  
  /* ===== 下拉框 ===== */
  --vscode-dropdown-background: #3C3C3C;
  --vscode-dropdown-foreground: #F0F0F0;
  --vscode-dropdown-border: #3C3C3C;
  
  /* ===== 复选框 ===== */
  --vscode-checkbox-background: #3C3C3C;
  --vscode-checkbox-foreground: #F0F0F0;
  --vscode-checkbox-border: #3C3C3C;
  
  /* ===== 滚动条 ===== */
  --vscode-scrollbar-shadow: #000000;
  --vscode-scrollbarSlider-background: rgba(121, 121, 121, 0.4);
  --vscode-scrollbarSlider-hoverBackground: rgba(100, 100, 100, 0.7);
  --vscode-scrollbarSlider-activeBackground: rgba(191, 191, 191, 0.4);
  
  /* ===== 进度条 ===== */
  --vscode-progressBar-background: #0E70C0;
  
  /* ===== 列表/树 ===== */
  --vscode-list-focusBackground: #062F4A;
  --vscode-list-focusForeground: #FFFFFF;
  --vscode-list-activeSelectionBackground: #094771;
  --vscode-list-activeSelectionForeground: #FFFFFF;
  --vscode-list-inactiveSelectionBackground: #37373D;
  --vscode-list-hoverBackground: #2A2D2E;
  --vscode-list-dropBackground: #383B3D;
  
  /* ===== 工具栏 ===== */
  --vscode-toolbar-hoverBackground: rgba(90, 93, 94, 0.31);
}
```

---

## 2. 基础组件 (Base Components)

### 2.1 Button 按钮

#### 接口定义

```typescript
interface IButtonOptions {
  title?: boolean | string;        // 悬停提示
  ariaLabel?: string;              // 无障碍标签
  supportIcons?: boolean;          // 支持图标（如 $(icon-name)）
  supportShortLabel?: boolean;     // 支持短标签
  secondary?: boolean;             // 次要按钮样式
  disabled?: boolean;              // 禁用状态
  hoverDelegate?: IHoverDelegate;  // 自定义悬停行为
}

interface IButtonStyles {
  buttonBackground: string;
  buttonHoverBackground: string;
  buttonForeground: string;
  buttonSeparator: string;
  buttonSecondaryBackground: string;
  buttonSecondaryHoverBackground: string;
  buttonSecondaryForeground: string;
  buttonBorder: string;
}
```

#### 默认样式值

```typescript
const unthemedButtonStyles = {
  buttonBackground: '#0E639C',
  buttonHoverBackground: '#006BB3',
  buttonForeground: '#FFFFFF',
  buttonSeparator: '#FFFFFF',
  buttonBorder: undefined,
  buttonSecondaryBackground: undefined,
  buttonSecondaryForeground: undefined,
  buttonSecondaryHoverBackground: undefined
};
```

#### CSS 样式

```css
.monaco-text-button {
  box-sizing: border-box;
  display: flex;
  width: 100%;
  padding: 4px;
  border-radius: 2px;
  text-align: center;
  cursor: pointer;
  justify-content: center;
  align-items: center;
  border: 1px solid var(--vscode-button-border, transparent);
  line-height: 18px;
}

.monaco-text-button:focus {
  outline-offset: 2px !important;
}

.monaco-button.disabled {
  opacity: 0.4 !important;
  cursor: default;
}

.monaco-text-button .codicon {
  margin: 0 0.2em;
  color: inherit !important;
}
```

#### 按钮变体

| 变体 | 类名 | 说明 |
|------|------|------|
| 主按钮 | `.monaco-text-button` | 蓝色背景 |
| 次要按钮 | `.monaco-text-button.secondary` | 灰色背景 |
| 带下拉按钮 | `.monaco-button-dropdown` | 主按钮 + 下拉箭头 |
| 带描述按钮 | `.monaco-description-button` | 标签 + 描述文字 |
| 禁用状态 | `.monaco-button.disabled` | 透明度 0.4 |

#### 使用示例 (纯 HTML/CSS)

```html
<a class="monaco-text-button" role="button" tabindex="0">
  <span>确定</span>
</a>

<a class="monaco-text-button secondary" role="button" tabindex="0">
  <span>取消</span>
</a>
```

```css
.monaco-text-button {
  display: inline-flex;
  padding: 4px 14px;
  border-radius: 2px;
  background-color: #0E639C;
  color: #FFFFFF;
  cursor: pointer;
  line-height: 18px;
  text-decoration: none;
}

.monaco-text-button:hover {
  background-color: #006BB3;
}

.monaco-text-button.secondary {
  background-color: #3A3D41;
}

.monaco-text-button.secondary:hover {
  background-color: #4a4d51;
}
```

### 2.2 IconLabel 图标标签

用于显示带图标的标签，常见于文件列表、树形视图等。

#### TypeScript 接口

```typescript
interface IIconLabelCreationOptions {
  readonly supportHighlights?: boolean;            // 支持高亮
  readonly supportDescriptionHighlights?: boolean; // 描述支持高亮
  readonly supportIcons?: boolean;                 // 支持图标
  readonly hoverDelegate?: IHoverDelegate;         // 悬停代理
}

interface IIconLabelValueOptions {
  title?: string;                    // 标题/悬停提示
  descriptionTitle?: string;         // 描述的悬停提示
  suffix?: string;                   // 后缀文字
  hideIcon?: boolean;                // 隐藏图标
  extraClasses?: readonly string[];  // 额外的 CSS 类
  bold?: boolean;                    // 粗体
  italic?: boolean;                  // 斜体
  strikethrough?: boolean;           // 删除线
  matches?: readonly IMatch[];       // 高亮匹配
  separator?: string;                // 分隔符，默认 '/'
}
```

#### 基础样式

```css
.monaco-icon-label {
  display: flex;
  overflow: hidden;
  text-overflow: ellipsis;
}

.monaco-icon-label > .monaco-icon-label-container {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  flex: 1;
}
```

#### 图标样式

```css
.monaco-icon-label::before {
  /* SVG 图标作为背景图 */
  background-size: 16px;
  background-position: left center;
  background-repeat: no-repeat;
  padding-right: 6px;
  width: 16px;
  height: 22px;
  line-height: inherit !important;
  display: inline-block;
  
  /* 字体图标 */
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  vertical-align: top;
  
  flex-shrink: 0;
}

/* 图标路径方式 */
.monaco-icon-label-iconpath {
  width: 16px;
  height: 22px;
  margin-right: 6px;
  display: flex;
}
```

#### 标签名称样式

```css
.monaco-icon-label > .monaco-icon-label-container > .monaco-icon-name-container > .label-name {
  color: inherit;
  white-space: pre;  /* 保留多个空格 */
}

/* 名称分隔符（如路径分隔） */
.monaco-icon-label > .monaco-icon-label-container > .monaco-icon-name-container > .label-name > .label-separator {
  margin: 0 2px;
  opacity: 0.5;
}
```

#### 后缀样式

```css
.monaco-icon-label > .monaco-icon-label-container > .monaco-icon-suffix-container > .label-suffix {
  opacity: .7;
  white-space: pre;
}
```

#### 描述样式

```css
.monaco-icon-label > .monaco-icon-label-container > .monaco-icon-description-container > .label-description {
  opacity: .7;
  margin-left: 0.5em;
  font-size: 0.9em;
  white-space: pre;
}

/* 浅色主题描述更明显 */
.vs .monaco-icon-label > .monaco-icon-label-container > .monaco-icon-description-container > .label-description {
  opacity: .95;
}
```

#### 文字样式修饰

```css
/* 粗体 */
.monaco-icon-label.bold > .monaco-icon-label-container > .monaco-icon-name-container > .label-name,
.monaco-icon-label.bold > .monaco-icon-label-container > .monaco-icon-description-container > .label-description {
  font-weight: bold;
}

/* 斜体 */
.monaco-icon-label.italic > .monaco-icon-label-container > .monaco-icon-name-container > .label-name,
.monaco-icon-label.italic > .monaco-icon-label-container > .monaco-icon-description-container > .label-description {
  font-style: italic;
}

/* 弃用样式 */
.monaco-icon-label.deprecated {
  text-decoration: line-through;
  opacity: 0.66;
}

/* 删除线 */
.monaco-icon-label.strikethrough > .monaco-icon-label-container > .monaco-icon-name-container > .label-name,
.monaco-icon-label.strikethrough > .monaco-icon-label-container > .monaco-icon-description-container > .label-description {
  text-decoration: line-through;
}
```

#### 禁用样式

```css
.monaco-icon-label-container.disabled {
  color: var(--vscode-disabledForeground);
}
```

#### 列表选中状态

```css
/* 选中时继承颜色 */
.monaco-list:focus .selected .monaco-icon-label,
.monaco-list:focus .selected .monaco-icon-label::after {
  color: inherit !important;
}

/* 选中时描述稍微透明 */
.monaco-list-row.focused.selected .label-description,
.monaco-list-row.selected .label-description {
  opacity: .8;
}
```

#### IconLabel 尺寸速查表

| 属性 | 值 |
|------|-----|
| 图标尺寸 | `16px × 16px` |
| 图标右边距 | `6px` |
| 图标高度 | `22px` |
| 描述左边距 | `0.5em` |
| 描述字号 | `0.9em` |
| 描述透明度 | `0.7` (深色) / `0.95` (浅色) |
| 分隔符边距 | `0 2px` |
| 分隔符透明度 | `0.5` |
| 后缀透明度 | `0.7` |
| 弃用透明度 | `0.66` |

### 2.3 KeybindingLabel 快捷键标签

用于显示键盘快捷键的组件。

#### TypeScript 接口

```typescript
interface IKeybindingLabelStyles {
  keybindingLabelBackground: string | undefined;      // 背景色
  keybindingLabelForeground: string | undefined;      // 前景色
  keybindingLabelBorder: string | undefined;          // 边框色
  keybindingLabelBottomBorder: string | undefined;    // 底部边框（立体效果）
  keybindingLabelShadow: string | undefined;          // 内阴影
}
```

#### 颜色变量

```css
:root {
  --vscode-keybindingLabel-background: rgba(128, 128, 128, 0.17);
  --vscode-keybindingLabel-foreground: #CCCCCC;
  --vscode-keybindingLabel-border: rgba(51, 51, 51, 0.6);
  --vscode-keybindingLabel-bottomBorder: rgba(68, 68, 68, 0.6);
}
```

#### 容器样式

```css
.monaco-keybinding {
  display: flex;
  align-items: center;
  line-height: 10px;
}
```

#### 按键样式

```css
.monaco-keybinding > .monaco-keybinding-key {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  
  /* 边框 */
  border-style: solid;
  border-width: 1px;
  border-radius: 3px;
  
  /* 尺寸 */
  min-width: 12px;
  font-size: 11px;
  padding: 3px 5px;
  margin: 0 2px;
}

/* 首尾按键边距 */
.monaco-keybinding > .monaco-keybinding-key:first-child {
  margin-left: 0;
}

.monaco-keybinding > .monaco-keybinding-key:last-child {
  margin-right: 0;
}
```

#### 分隔符样式

```css
/* 按键分隔符（如 Ctrl + S 中的 +） */
.monaco-keybinding > .monaco-keybinding-key-separator {
  display: inline-block;
}

/* 和弦分隔符（如 Ctrl+K Ctrl+S 中间的空格） */
.monaco-keybinding > .monaco-keybinding-key-chord-separator {
  width: 6px;
}
```

#### 立体效果实现

```typescript
// 通过 boxShadow 实现立体效果
if (this.options.keybindingLabelShadow) {
  keyElement.style.boxShadow = `inset 0 -1px 0 ${this.options.keybindingLabelShadow}`;
}
```

#### KeybindingLabel 尺寸速查表

| 属性 | 值 |
|------|-----|
| 按键最小宽度 | `12px` |
| 按键字号 | `11px` |
| 按键内边距 | `3px 5px` |
| 按键边距 | `0 2px` |
| 按键圆角 | `3px` |
| 按键边框宽度 | `1px` |
| 容器行高 | `10px` |
| 和弦分隔宽度 | `6px` |

### 2.4 HighlightedLabel 高亮标签

用于显示带搜索高亮的文本标签。

#### TypeScript 接口

```typescript
interface IHighlight {
  start: number;           // 高亮起始位置
  end: number;             // 高亮结束位置
  readonly extraClasses?: readonly string[];  // 额外的 CSS 类
}

// 使用方法
set(
  text: string | undefined,           // 文本内容
  highlights?: readonly IHighlight[], // 高亮范围数组
  title?: string,                     // 悬停提示
  escapeNewLines?: boolean,           // 转义换行符
  supportIcons?: boolean              // 支持图标
)
```

#### 基础样式

```css
.monaco-highlighted-label {
  /* 容器样式由外部定义 */
}

/* 高亮区域 */
.monaco-highlighted-label .highlight {
  /* 高亮样式通常由主题定义 */
  font-weight: bold;
  /* 或使用背景色高亮 */
}
```

#### 高亮颜色

```css
:root {
  /* 列表中的高亮 */
  --vscode-list-highlightForeground: #2AAAFF;  /* dark */
  /* light: #0066BF */
  
  /* 焦点项的高亮 */
  --vscode-list-focusHighlightForeground: #2AAAFF;
}
```

#### 换行符转义

当 `escapeNewLines` 为 `true` 时，换行符会被替换为 `⏎` (U+23CE) 符号。

### 2.5 CountBadge 计数徽章

用于显示数字计数的小型徽章组件，常见于侧边栏、标签页等位置。

#### TypeScript 接口

```typescript
interface ICountBadgeOptions {
  readonly count?: number;           // 显示的数字
  readonly countFormat?: string;     // 格式化模板，默认 '{0}'
  readonly titleFormat?: string;     // 悬停提示模板
}

interface ICountBadgeStyles {
  readonly badgeBackground: string | undefined;   // 背景色
  readonly badgeForeground: string | undefined;   // 前景色
  readonly badgeBorder: string | undefined;       // 边框色
}
```

#### 默认颜色

```css
:root {
  --vscode-badge-background: #4D4D4D;   /* dark: #4D4D4D, light: #C4C4C4 */
  --vscode-badge-foreground: #FFFFFF;   /* dark: #FFFFFF, light: #333333 */
}
```

#### CSS 样式规范

```css
.monaco-count-badge {
  /* 尺寸 */
  padding: 3px 5px;
  min-width: 18px;
  min-height: 18px;
  
  /* 圆形外观 */
  border-radius: 11px;
  
  /* 字体 */
  font-size: 11px;
  line-height: 11px;
  font-weight: normal;
  text-align: center;
  
  /* 布局 */
  display: inline-block;
  box-sizing: border-box;
}

/* 长数字变体 - 变为圆角矩形 */
.monaco-count-badge.long {
  padding: 2px 3px;
  border-radius: 2px;
  min-height: auto;
  line-height: normal;
}
```

#### 尺寸速查表

| 变体 | 内边距 | 最小尺寸 | 圆角 | 行高 |
|------|--------|----------|------|------|
| 默认（圆形） | `3px 5px` | `18px × 18px` | `11px` | `11px` |
| 长数字 | `2px 3px` | 自适应 | `2px` | `normal` |

### 2.6 Separator & Sash 分割线与拖拽条

VS Code 中有多种分割线/分隔符类型：

#### Sash 可拖拽分割条

用于面板之间的可拖拽边界（如侧边栏与编辑器之间）。

**尺寸变量**
```css
:root {
  --vscode-sash-size: 4px;         /* 默认尺寸 */
  --vscode-sash-hover-size: 4px;   /* 悬停时可见区域 */
}
```

**颜色变量**
```css
:root {
  /* 悬停/激活时的边框颜色（继承 focusBorder） */
  --vscode-sash-hoverBorder: #007FD4;  /* dark: #007FD4, light: #0090F1 */
}
```

**CSS 样式**
```css
.monaco-sash {
  position: absolute;
  z-index: 35;
  touch-action: none;
}

/* 垂直分割条 */
.monaco-sash.vertical {
  cursor: ew-resize;
  top: 0;
  width: var(--vscode-sash-size);  /* 4px */
  height: 100%;
}

/* 水平分割条 */
.monaco-sash.horizontal {
  cursor: ns-resize;
  left: 0;
  width: 100%;
  height: var(--vscode-sash-size);  /* 4px */
}

/* 悬停效果 */
.monaco-sash:before {
  content: '';
  pointer-events: none;
  position: absolute;
  width: 100%;
  height: 100%;
  background: transparent;
}

.monaco-sash.hover:before,
.monaco-sash.active:before {
  background: var(--vscode-sash-hoverBorder);
}

/* 悬停时的可见区域定位 */
.monaco-sash.vertical:before {
  width: var(--vscode-sash-hover-size);
  left: calc(50% - (var(--vscode-sash-hover-size) / 2));
}

.monaco-sash.horizontal:before {
  height: var(--vscode-sash-hover-size);
  top: calc(50% - (var(--vscode-sash-hover-size) / 2));
}

/* 过渡动画 */
.monaco-enable-motion .monaco-sash:before {
  transition: background-color 0.1s ease-out;
}

/* 禁用状态 */
.monaco-sash.disabled {
  pointer-events: none;
  cursor: default !important;
}
```

#### Menu Separator 菜单分隔线

菜单中的水平分隔线。

**颜色**
```css
:root {
  --vscode-menu-separatorBackground: #606060;  /* dark: #606060, light: #D4D4D4 */
}
```

**CSS 样式**
```css
.monaco-menu .separator {
  border-bottom: 1px solid var(--vscode-menu-separatorBackground);
  padding-top: 1px;
}
```

#### ActionBar Separator 操作栏分隔符

工具栏/操作栏中的垂直分隔线。

**尺寸与颜色**

| 属性 | 值 |
|------|-----|
| 宽度 | `1px` |
| 高度 | `16px` |
| 外边距 | `5px 4px` |
| 颜色 | `var(--vscode-disabledForeground)` |

**CSS 样式**
```css
/* 水平操作栏中的垂直分隔符 */
.monaco-action-bar .action-item .action-label.separator {
  width: 1px;
  height: 16px;
  margin: 5px 4px !important;
  cursor: default;
  min-width: 1px;
  padding: 0;
  background-color: var(--vscode-disabledForeground);
}

/* 垂直操作栏中的水平分隔符 */
.monaco-action-bar.vertical .action-label.separator {
  display: block;
  border-bottom: 1px solid var(--vscode-disabledForeground);
  padding-top: 1px;
  margin-left: .8em;
  margin-right: .8em;
}
```

#### Button Separator 按钮分隔符

下拉按钮中主按钮与箭头之间的分隔线。

**颜色**
```css
:root {
  --vscode-button-separator: rgba(255, 255, 255, 0.4);  /* 40% 透明白色 */
}
```

**CSS 样式**
```css
.monaco-button-dropdown .monaco-button-dropdown-separator > div {
  width: 1px;
  height: 100%;
  background-color: var(--vscode-button-separator);
}
```

#### Text Separator 文本分隔符

文本内容中的分隔符颜色。

**颜色**
```css
:root {
  --vscode-textSeparator-foreground: rgba(255, 255, 255, 0.18);  /* dark */
  /* light: rgba(0, 0, 0, 0.18) */
}
```

#### 分隔符颜色汇总表

| 变量 | 深色主题 | 浅色主题 | 用途 |
|------|----------|----------|------|
| `--vscode-sash-hoverBorder` | `#007FD4` | `#0090F1` | 拖拽条悬停 |
| `--vscode-menu-separatorBackground` | `#606060` | `#D4D4D4` | 菜单分隔线 |
| `--vscode-disabledForeground` | `#CCCCCC80` | `#61616180` | 操作栏分隔符 |
| `--vscode-button-separator` | `rgba(255,255,255,0.4)` | - | 下拉按钮分隔符 |
| `--vscode-textSeparator-foreground` | `rgba(255,255,255,0.18)` | `rgba(0,0,0,0.18)` | 文本分隔符 |

#### 分隔符尺寸汇总表

| 类型 | 宽度 | 高度 | 其他 |
|------|------|------|------|
| Sash 拖拽条 | `4px` | `100%` | 水平/垂直 |
| 菜单分隔线 | `100%` | `1px` | `border-bottom` |
| 操作栏垂直分隔符 | `1px` | `16px` | `margin: 5px 4px` |
| 操作栏水平分隔符 | `auto` | `1px` | `margin: 0 .8em` |
| 按钮分隔符 | `1px` | `100%` | 继承按钮高度 |

---

## 3. 表单与输入 (Form & Input)

### 3.1 InputBox 输入框

#### 接口定义

```typescript
interface IInputOptions {
  placeholder?: string;            // 占位符
  showPlaceholderOnFocus?: boolean;// 聚焦时显示占位符
  tooltip?: string;                // 工具提示
  ariaLabel?: string;              // 无障碍标签
  type?: string;                   // 输入类型 (text/password)
  flexibleHeight?: boolean;        // 自适应高度（textarea）
  flexibleWidth?: boolean;         // 自适应宽度
  flexibleMaxHeight?: number;      // 最大高度
  actions?: IAction[];             // 右侧操作按钮
  validationOptions?: {            // 验证配置
    validation?: (value: string) => IMessage | null;
  };
  inputBoxStyles: IInputBoxStyles;
  history?: IHistory<string>;      // 历史记录
}

interface IInputBoxStyles {
  inputBackground: string;
  inputForeground: string;
  inputBorder: string;
  inputValidationInfoBorder: string;
  inputValidationInfoBackground: string;
  inputValidationInfoForeground: string;
  inputValidationWarningBorder: string;
  inputValidationWarningBackground: string;
  inputValidationWarningForeground: string;
  inputValidationErrorBorder: string;
  inputValidationErrorBackground: string;
  inputValidationErrorForeground: string;
}
```

#### 默认样式值

```typescript
const unthemedInboxStyles = {
  inputBackground: '#3C3C3C',
  inputForeground: '#CCCCCC',
  inputBorder: undefined,
  inputValidationInfoBorder: '#55AAFF',
  inputValidationInfoBackground: '#063B49',
  inputValidationWarningBorder: '#B89500',
  inputValidationWarningBackground: '#352A05',
  inputValidationErrorBorder: '#BE1100',
  inputValidationErrorBackground: '#5A1D1D',
};
```

#### CSS 样式

```css
.monaco-inputbox {
  position: relative;
  display: block;
  padding: 0;
  box-sizing: border-box;
  border-radius: 2px;
  font-size: inherit;
}

.monaco-inputbox > .ibwrapper > .input {
  padding: 4px 6px;
  display: inline-block;
  box-sizing: border-box;
  width: 100%;
  height: 100%;
  line-height: inherit;
  border: none;
  font-family: inherit;
  font-size: inherit;
  resize: none;
  color: inherit;
}

.monaco-inputbox > .ibwrapper > input {
  text-overflow: ellipsis;
}

/* 验证消息 */
.monaco-inputbox-container .monaco-inputbox-message {
  display: inline-block;
  overflow: hidden;
  text-align: left;
  width: 100%;
  padding: 0.4em;
  font-size: 12px;
  line-height: 17px;
  margin-top: -1px;
  word-wrap: break-word;
}

/* 操作栏 */
.monaco-inputbox .monaco-action-bar {
  position: absolute;
  right: 2px;
  top: 4px;
}
```

#### 验证状态

| 状态 | 边框颜色 | 背景颜色 |
|------|----------|----------|
| Info | `#55AAFF` | `#063B49` |
| Warning | `#B89500` | `#352A05` |
| Error | `#BE1100` | `#5A1D1D` |

### 3.2 FindInput 查找输入框

带有搜索选项开关的输入框组件，用于搜索/替换功能。

#### TypeScript 接口

```typescript
interface IFindInputOptions {
  readonly placeholder?: string;           // 占位符
  readonly width?: number;                 // 宽度
  readonly validation?: IInputValidator;  // 验证器
  readonly label: string;                  // 标签
  readonly flexibleHeight?: boolean;       // 灵活高度
  readonly flexibleWidth?: boolean;        // 灵活宽度
  readonly flexibleMaxHeight?: number;     // 最大高度
  
  // 内置开关
  readonly showCommonFindToggles?: boolean;    // 显示常用开关
  readonly appendCaseSensitiveLabel?: string;  // 大小写敏感标签
  readonly appendWholeWordsLabel?: string;     // 全词匹配标签
  readonly appendRegexLabel?: string;          // 正则表达式标签
  readonly additionalToggles?: Toggle[];       // 额外的开关
  readonly showHistoryHint?: () => boolean;    // 显示历史提示
  
  readonly toggleStyles: IToggleStyles;
  readonly inputBoxStyles: IInputBoxStyles;
  readonly history?: IHistory<string>;         // 搜索历史
  readonly hoverLifecycleOptions?: IHoverLifecycleOptions; // 悬停生命周期选项
}
```

#### 基础样式

```css
.monaco-findInput {
  position: relative;
}

.monaco-findInput .monaco-inputbox {
  font-size: 13px;
  width: 100%;
}
```

#### 控件区域（开关按钮）

```css
.monaco-findInput > .controls {
  position: absolute;
  top: 3px;
  right: 2px;
}
```

#### 禁用状态

```css
/* 浅色主题 */
.vs .monaco-findInput.disabled {
  background-color: #E1E1E1;
}

/* 深色主题 */
.vs-dark .monaco-findInput.disabled {
  background-color: #333;
}
```

#### 高亮动画

当切换选项时的闪烁提示效果。

```css
/* 浅色主题高亮 */
@keyframes monaco-findInput-highlight-0 {
  0% { background: rgba(253, 255, 0, 0.8); }  /* 黄色 */
  100% { background: transparent; }
}

/* 深色主题高亮 */
@keyframes monaco-findInput-highlight-dark-0 {
  0% { background: rgba(255, 255, 255, 0.44); }  /* 白色半透明 */
  100% { background: transparent; }
}

.monaco-findInput.highlight-0 .controls,
.hc-light .monaco-findInput.highlight-0 .controls {
  animation: monaco-findInput-highlight-0 100ms linear 0s;
}

.hc-black .monaco-findInput.highlight-0 .controls,
.vs-dark .monaco-findInput.highlight-0 .controls {
  animation: monaco-findInput-highlight-dark-0 100ms linear 0s;
}
```

#### 内置开关按钮

FindInput 内置三个常用开关：

| 开关 | 类名 | 功能 |
|------|------|------|
| `CaseSensitiveToggle` | `.codicon-case-sensitive` | 区分大小写 |
| `WholeWordsToggle` | `.codicon-whole-word` | 全词匹配 |
| `RegexToggle` | `.codicon-regex` | 正则表达式 |

#### FindInput 尺寸速查表

| 属性 | 值 |
|------|-----|
| 输入框字号 | `13px` |
| 控件区域上边距 | `3px` |
| 控件区域右边距 | `2px` |
| 高亮动画时长 | `100ms` |

### 3.3 SelectBox 下拉选择框

#### 接口定义

```typescript
interface ISelectBoxOptions {
  useCustomDrawn?: boolean;        // 使用自定义绘制（非原生）
  ariaLabel?: string;
  ariaDescription?: string;
  minBottomMargin?: number;        // 下拉列表最小底部边距
  optionsAsChildren?: boolean;     // 选项作为子元素
}

interface ISelectOptionItem {
  text: string;                    // 显示文本
  detail?: string;                 // 详细信息
  decoratorRight?: string;         // 右侧装饰
  description?: string;            // 描述
  descriptionIsMarkdown?: boolean; // 描述是否为 Markdown
  readonly descriptionMarkdownActionHandler?: MarkdownActionHandler; // Markdown 动作处理器
  isDisabled?: boolean;            // 是否禁用
}

interface ISelectBoxStyles {
  selectBackground: string;
  selectListBackground: string;
  selectForeground: string;
  decoratorRightForeground: string;
  selectBorder: string;
  selectListBorder: string;
  focusBorder: string;
  // 继承列表样式...
}
```

#### 默认样式值

```typescript
const unthemedSelectBoxStyles = {
  selectBackground: '#3C3C3C',
  selectForeground: '#F0F0F0',
  selectBorder: '#3C3C3C',
  decoratorRightForeground: undefined,
  selectListBackground: undefined,
  selectListBorder: undefined,
  focusBorder: undefined,
};
```

#### CSS 样式

```css
.monaco-select-box {
  width: 100%;
  cursor: pointer;
  border-radius: 2px;
}

.monaco-select-box-dropdown-container {
  font-size: 13px;
  font-weight: normal;
  text-transform: none;
}

/* 在 ActionBar 中的样式 */
.monaco-action-bar .action-item .monaco-select-box {
  cursor: pointer;
  min-width: 100px;
  min-height: 18px;
  padding: 2px 23px 2px 8px;
}

/* macOS 特殊样式 */
.mac .monaco-action-bar .action-item .monaco-select-box {
  font-size: 11px;
  border-radius: 3px;
  min-height: 24px;
}
```

#### 分隔符选项

```typescript
// 使用预定义的分隔符
const SeparatorSelectOption = {
  text: '─────────',  // Unicode 横线字符
  isDisabled: true
};
```

### 3.4 Dropdown 下拉菜单

用于触发下拉菜单的组件。

#### 基础样式

```css
.monaco-dropdown {
  height: 100%;
  padding: 0;
}
```

#### 下拉标签样式

```css
.monaco-dropdown > .dropdown-label {
  cursor: pointer;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 禁用状态 */
.monaco-dropdown > .dropdown-label > .action-label.disabled {
  cursor: default;
}
```

#### 带主按钮的下拉组合

用于分离式按钮（主操作 + 下拉箭头）。

```css
.monaco-dropdown-with-primary {
  display: flex !important;
  flex-direction: row;
  border-radius: 5px;
}

.monaco-dropdown-with-primary > .action-container > .action-label {
  margin-right: 0;
}

/* 下拉箭头图标 */
.monaco-dropdown-with-primary > .dropdown-action-container > .monaco-dropdown > .dropdown-label .codicon[class*='codicon-'] {
  font-size: 12px;
  padding-left: 0px;
  padding-right: 0px;
  line-height: 16px;
  margin-left: -3px;
}

/* 下拉按钮 */
.monaco-dropdown-with-primary > .dropdown-action-container > .monaco-dropdown > .dropdown-label > .action-label {
  display: block;
  background-size: 16px;
  background-position: center center;
  background-repeat: no-repeat;
}
```

#### Dropdown 尺寸速查表

| 属性 | 值 |
|------|-----|
| 组合按钮圆角 | `5px` |
| 箭头图标字号 | `12px` |
| 箭头行高 | `16px` |
| 背景图尺寸 | `16px` |

### 3.5 Radio 单选按钮组

按钮式单选组件，用于在多个选项中选择一个。

#### TypeScript 接口

```typescript
interface IRadioStyles {
  readonly activeForeground?: string;        // 激活前景色
  readonly activeBackground?: string;        // 激活背景色
  readonly activeBorder?: string;            // 激活边框色
  readonly inactiveForeground?: string;      // 非激活前景色
  readonly inactiveBackground?: string;      // 非激活背景色
  readonly inactiveHoverBackground?: string; // 非激活悬停背景色
  readonly inactiveBorder?: string;          // 非激活边框色
}

interface IRadioOptionItem {
  readonly text: string;       // 显示文本
  readonly tooltip?: string;   // 悬停提示
  readonly isActive?: boolean; // 是否激活
  readonly disabled?: boolean; // 是否禁用
}

interface IRadioOptions {
  readonly items: ReadonlyArray<IRadioOptionItem>;  // 选项列表
  readonly activeIcon?: ThemeIcon;                   // 激活图标
  readonly hoverDelegate?: IHoverDelegate;           // 悬停代理
}
```

#### 颜色变量

```css
:root {
  /* 激活状态 */
  --vscode-radio-activeForeground: /* 继承按钮前景色 */;
  --vscode-radio-activeBackground: /* 继承按钮背景色 */;
  --vscode-radio-activeBorder: transparent;
  
  /* 非激活状态 */
  --vscode-radio-inactiveForeground: /* 继承按钮次要前景色 */;
  --vscode-radio-inactiveBackground: /* 继承按钮次要背景色 */;
  --vscode-radio-inactiveHoverBackground: /* 自动计算悬停色 */;
  --vscode-radio-inactiveBorder: transparent;
}
```

#### 容器样式

```css
.monaco-custom-radio {
  display: flex;
}
```

#### 按钮样式

```css
.monaco-custom-radio > .monaco-button {
  border-radius: 0;          /* 中间按钮无圆角 */
  font-size: 0.9em;
  line-height: 1em;
  padding-left: 0.5em;
  padding-right: 0.5em;
}

/* 第一个按钮左侧圆角 */
.monaco-custom-radio > .monaco-button:first-child {
  border-top-left-radius: 3px;
  border-bottom-left-radius: 3px;
}

/* 最后一个按钮右侧圆角 */
.monaco-custom-radio > .monaco-button:last-child {
  border-top-right-radius: 3px;
  border-bottom-right-radius: 3px;
}
```

#### 边框处理

```css
/* 非激活且非末尾的按钮去除右边框（避免双边框） */
.monaco-custom-radio > .monaco-button:not(.active):not(:last-child) {
  border-right: none;
}

/* 激活按钮后面的按钮去除左边框 */
.monaco-custom-radio > .monaco-button.previous-active {
  border-left: none;
}
```

#### 状态样式

```css
/* 非激活状态 */
.monaco-custom-radio > .monaco-button {
  color: var(--vscode-radio-inactiveForeground);
  background-color: var(--vscode-radio-inactiveBackground);
  border-color: var(--vscode-radio-inactiveBorder, transparent);
}

/* 激活状态 */
.monaco-custom-radio > .monaco-button.active:hover,
.monaco-custom-radio > .monaco-button.active {
  color: var(--vscode-radio-activeForeground);
  background-color: var(--vscode-radio-activeBackground);
  border-color: var(--vscode-radio-activeBorder, transparent);
}

/* 悬停状态（非激活） */
.monaco-custom-radio > .monaco-button:hover:not(.active) {
  background-color: var(--vscode-radio-inactiveHoverBackground);
}
```

#### 高对比度模式

```css
.hc-black .monaco-custom-radio > .monaco-button.active,
.hc-light .monaco-custom-radio > .monaco-button.active {
  border-color: var(--vscode-radio-activeBorder, transparent);
}

.hc-black .monaco-custom-radio > .monaco-button:not(.active),
.hc-light .monaco-custom-radio > .monaco-button:not(.active) {
  border-color: var(--vscode-radio-inactiveBorder, transparent);
}

/* 高对比度悬停虚线边框 */
.hc-black .monaco-custom-radio > .monaco-button:not(.active):hover,
.hc-light .monaco-custom-radio > .monaco-button:not(.active):hover {
  outline: 1px dashed var(--vscode-toolbar-hoverOutline);
  outline-offset: -1px;
}
```

#### Radio 尺寸速查表

| 属性 | 值 |
|------|-----|
| 字号 | `0.9em` |
| 行高 | `1em` |
| 左右内边距 | `0.5em` |
| 圆角（首尾） | `3px` |
| 圆角（中间） | `0` |

### 3.6 Toggle & Checkbox 开关与复选框

#### Toggle 接口

```typescript
interface IToggleOpts {
  actionClassName?: string;        // 额外 CSS 类
  icon?: ThemeIcon;                // 图标
  title: string;                   // 标题/提示
  isChecked: boolean;              // 是否选中
  notFocusable?: boolean;          // 不可聚焦
  // 样式
  inputActiveOptionBorder: string;
  inputActiveOptionForeground: string;
  inputActiveOptionBackground: string;
}
```

#### Checkbox 接口

```typescript
interface ICheckboxStyles {
  checkboxBackground: string;
  checkboxBorder: string;
  checkboxForeground: string;
  checkboxDisabledBackground: string;
  checkboxDisabledForeground: string;
  size?: number;  // 默认 18px
}
```

#### CSS 样式

```css
.monaco-custom-toggle {
  margin-left: 2px;
  float: left;
  cursor: pointer;
  overflow: hidden;
  width: 20px;
  height: 20px;
  border-radius: 3px;
  border: 1px solid transparent;
  padding: 1px;
  box-sizing: border-box;
  user-select: none;
}

.monaco-custom-toggle:hover {
  background-color: var(--vscode-inputOption-hoverBackground);
}

.monaco-custom-toggle.monaco-checkbox {
  height: 18px;
  width: 18px;
  border: 1px solid transparent;
  border-radius: 3px;
  margin-right: 9px;
  margin-left: 0px;
  padding: 0px;
}

/* 选中状态通过添加 .checked 类控制 */
```

#### 变体

| 组件 | 说明 |
|------|------|
| `Toggle` | 单个开关按钮 |
| `Checkbox` | 复选框（带勾选图标） |
| `TriStateCheckbox` | 三态复选框（未选/已选/部分选中） |

---

## 4. 导航与操作 (Navigation & Actions)

### 4.1 Menu 菜单

#### 接口定义

```typescript
interface IMenuOptions {
  context?: unknown;               // 上下文数据
  actionViewItemProvider?: IActionViewItemProvider;
  actionRunner?: IActionRunner;
  getKeyBinding?: (action) => ResolvedKeybinding;
  ariaLabel?: string;
  enableMnemonics?: boolean;       // 启用助记符（Alt+字母）
  anchorAlignment?: AnchorAlignment;
  expandDirection?: IMenuDirection;
  useEventAsContext?: boolean;
  submenuIds?: Set<string>;
}

interface IMenuStyles {
  shadowColor: string;
  borderColor: string;
  foregroundColor: string;
  backgroundColor: string;
  selectionForegroundColor: string;
  selectionBackgroundColor: string;
  selectionBorderColor: string;
  separatorColor: string;
  scrollbarShadow: string;
  scrollbarSliderBackground: string;
  scrollbarSliderHoverBackground: string;
  scrollbarSliderActiveBackground: string;
}
```

#### CSS 样式（Menubar）

```css
.menubar {
  display: flex;
  flex-shrink: 1;
  box-sizing: border-box;
  height: 100%;
  overflow: hidden;
}

.menubar > .menubar-menu-button {
  display: flex;
  align-items: center;
  box-sizing: border-box;
  cursor: default;
  -webkit-app-region: no-drag;
  white-space: nowrap;
  outline: 0 !important;
}

.menubar-menu-title {
  padding: 0px 8px;
  border-radius: 5px;
}

.menubar .menubar-menu-items-holder {
  position: fixed;
  left: 0px;
  opacity: 1;
  z-index: 2000;
}
```

#### 方向配置

```typescript
enum HorizontalDirection {
  Right = 0,
  Left = 1
}

enum VerticalDirection {
  Above = 0,
  Below = 1
}
```

### 4.2 Toolbar 工具栏

#### 接口定义

```typescript
interface IToolBarOptions {
  orientation?: ActionsOrientation;     // 方向
  actionViewItemProvider?: IActionViewItemProvider;
  ariaLabel?: string;
  getKeyBinding?: (action) => ResolvedKeybinding;
  actionRunner?: IActionRunner;
  toggleMenuTitle?: string;             // 更多菜单标题
  anchorAlignmentProvider?: () => AnchorAlignment;
  renderDropdownAsChildElement?: boolean;
  moreIcon?: ThemeIcon;                 // 更多按钮图标
  allowContextMenu?: boolean;
  skipTelemetry?: boolean;
  hoverDelegate?: IHoverDelegate;
  highlightToggledItems?: boolean;      // 高亮切换项
  icon?: boolean;                       // 显示图标（默认 true）
  label?: boolean;                      // 显示标签（默认 false）
  responsiveBehavior?: {                // 响应式行为
    enabled: boolean;
    minItems?: number;
  };
}
```

#### CSS 样式

```css
.monaco-toolbar {
  height: 100%;
}

.monaco-toolbar .toolbar-toggle-more {
  display: inline-block;
  padding: 0;
}

/* 响应式模式 */
.monaco-toolbar.responsive .monaco-action-bar > .actions-container > .action-item {
  flex-shrink: 1;
  min-width: 20px;
}
```

### 4.3 Breadcrumbs 面包屑导航

用于显示层级路径的导航组件，常见于编辑器顶部。

#### TypeScript 接口

```typescript
interface IBreadcrumbsWidgetStyles {
  readonly breadcrumbsBackground: string | undefined;              // 背景色
  readonly breadcrumbsForeground: string | undefined;              // 默认前景色
  readonly breadcrumbsHoverForeground: string | undefined;         // 悬停前景色
  readonly breadcrumbsFocusForeground: string | undefined;         // 焦点前景色
  readonly breadcrumbsFocusAndSelectionForeground: string | undefined; // 焦点+选中前景色
}

interface IBreadcrumbsItemEvent {
  type: 'select' | 'focus';
  item: BreadcrumbsItem;
  node: HTMLElement;
  payload: unknown;
}
```

#### 颜色变量

```css
:root {
  --vscode-breadcrumb-background: transparent;
  --vscode-breadcrumb-foreground: #CCCCCC99;           /* dark: 60% 透明 */
  --vscode-breadcrumb-focusForeground: #E0E0E0;
  --vscode-breadcrumbPicker-background: #252526;       /* 下拉选择器背景 */
}
```

#### 容器样式

```css
.monaco-breadcrumbs {
  user-select: none;
  -webkit-user-select: none;
  
  /* Flexbox 布局 */
  display: flex;
  flex-direction: row;
  flex-wrap: nowrap;
  justify-content: flex-start;
  
  outline-style: none;
}
```

#### 面包屑项样式

```css
.monaco-breadcrumbs .monaco-breadcrumb-item {
  display: flex;
  align-items: center;
  flex: 0 1 auto;
  
  white-space: nowrap;
  cursor: pointer;
  align-self: center;
  height: 100%;
  outline: none;
}

/* 禁用状态 */
.monaco-breadcrumbs.disabled .monaco-breadcrumb-item {
  cursor: default;
}

/* 分隔符颜色继承 */
.monaco-breadcrumbs .monaco-breadcrumb-item .codicon-breadcrumb-separator {
  color: inherit;
}

/* 第一项前添加空格 */
.monaco-breadcrumbs .monaco-breadcrumb-item:first-of-type::before {
  content: ' ';
}
```

#### Breadcrumbs 特性

| 特性 | 说明 |
|------|------|
| 水平滚动 | 内置 `DomScrollableElement`，支持水平滚动 |
| 键盘导航 | 支持焦点/选中状态切换 |
| 自定义分隔符 | 通过 `ThemeIcon` 自定义分隔符图标 |
| ARIA 支持 | `role="list"`，项为 listitem |

---

## 5. 数据展示与布局 (Data Display & Layout)

### 5.1 List 列表

#### CSS 样式

```css
.monaco-list {
  position: relative;
  height: 100%;
  width: 100%;
  white-space: nowrap;
}

.monaco-list.mouse-support {
  user-select: none;
  -webkit-user-select: none;
}

.monaco-list-rows {
  position: relative;
  width: 100%;
  height: 100%;
}

.monaco-list-row {
  position: absolute;
  box-sizing: border-box;
  overflow: hidden;
  width: 100%;
}

.monaco-list.mouse-support .monaco-list-row {
  cursor: pointer;
  touch-action: none;
}

/* 水平滚动 */
.monaco-list.horizontal-scrolling .monaco-list-rows {
  width: auto;
  min-width: 100%;
}

/* 滚动条 z-index */
.monaco-list .monaco-scrollable-element > .scrollbar.vertical {
  z-index: 14;
}

/* 筛选消息 */
.monaco-list-type-filter-message {
  position: absolute;
  width: 100%;
  height: 100%;
  padding: 40px 1em 1em 1em;
  text-align: center;
  white-space: normal;
  opacity: 0.7;
  pointer-events: none;
}
```

#### 列表样式接口

```typescript
interface IListStyles {
  listBackground: string;
  listFocusBackground: string;
  listFocusForeground: string;
  listActiveSelectionBackground: string;
  listActiveSelectionForeground: string;
  listFocusAndSelectionBackground: string;
  listFocusAndSelectionForeground: string;
  listInactiveSelectionBackground: string;
  listInactiveSelectionForeground: string;
  listHoverBackground: string;
  listHoverForeground: string;
  listDropOverBackground: string;
  listDropBetweenBackground: string;
  listFocusOutline: string;
  listInactiveFocusOutline: string;
  listSelectionOutline: string;
  listHoverOutline: string;
  // 树形结构
  treeIndentGuidesStroke: string;
  treeInactiveIndentGuidesStroke: string;
  // 表格
  tableColumnsBorder: string;
  tableOddRowsBackground: string;
}
```

### 5.2 Tree 树形控件

高性能虚拟滚动树形组件。

#### 行样式

```css
.monaco-tl-row {
  display: flex;
  height: 100%;
  align-items: center;
  position: relative;
}

.monaco-tl-row.disabled {
  cursor: default;
}
```

#### 缩进线

```css
.monaco-tl-indent {
  height: 100%;
  position: absolute;
  top: 0;
  left: 16px;
  pointer-events: none;
}

/* 隐藏箭头时调整位置 */
.hide-arrows .monaco-tl-indent {
  left: 12px;
}

/* 缩进引导线 */
.monaco-tl-indent > .indent-guide {
  display: inline-block;
  box-sizing: border-box;
  height: 100%;
  border-left: 1px solid transparent;
  opacity: 0;
}

/* 动画过渡 */
.monaco-enable-motion .monaco-tl-indent > .indent-guide {
  transition: opacity 0.1s linear;
}
```

#### 缩进线颜色

```css
:root {
  --vscode-tree-indentGuidesStroke: #585858;         /* dark */
  /* light: #a9a9a9 */
  
  --vscode-tree-inactiveIndentGuidesStroke: /* 40% 透明度 */;
}
```

#### 展开/折叠箭头（Twistie）

```css
.monaco-tl-twistie {
  font-size: 10px;
  text-align: right;
  padding-right: 6px;
  flex-shrink: 0;
  width: 16px;
  display: flex !important;
  align-items: center;
  justify-content: center;
  transform: translateX(3px);
}

.monaco-tl-twistie,
.monaco-tl-contents {
  height: 100%;
}

/* 箭头圆形背景 */
.monaco-tl-twistie::before {
  border-radius: 20px;
}

/* 折叠状态旋转 */
.monaco-tl-twistie.collapsed::before {
  transform: rotate(-90deg);
}

/* 加载动画 */
.monaco-tl-twistie.codicon-tree-item-loading::before {
  animation: codicon-spin 1.25s steps(30) infinite;
}
```

#### 内容区域

```css
.monaco-tl-contents {
  flex: 1;
  overflow: hidden;
}
```

#### 筛选器组件

```css
.monaco-tree-type-filter {
  position: absolute;
  top: 0;
  right: 0;
  display: flex;
  padding: 3px;
  max-width: 200px;
  z-index: 100;
  margin: 0 10px 0 6px;
  border: 1px solid var(--vscode-widget-border);
  border-bottom-left-radius: 4px;
  border-bottom-right-radius: 4px;
}

/* 动画过渡 */
.monaco-enable-motion .monaco-tree-type-filter {
  transition: top 0.3s;
}

/* 禁用状态 */
.monaco-tree-type-filter.disabled {
  top: -40px !important;
}
```

#### 筛选器输入框

```css
.monaco-tree-type-filter-input {
  flex: 1;
}

.monaco-tree-type-filter-input .monaco-inputbox {
  height: 23px;
}

.monaco-tree-type-filter-input .monaco-inputbox > .ibwrapper > .input,
.monaco-tree-type-filter-input .monaco-inputbox > .ibwrapper > .mirror {
  padding: 2px 4px;
}

.monaco-tree-type-filter-input .monaco-findInput > .controls {
  top: 2px;
}
```

#### 筛选器操作栏

```css
.monaco-tree-type-filter-actionbar {
  margin-left: 4px;
}

.monaco-tree-type-filter-actionbar .monaco-action-bar .action-label {
  padding: 2px;
}
```

#### 粘性滚动容器

```css
.monaco-list .monaco-scrollable-element .monaco-tree-sticky-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 0;
  z-index: 13;
  background-color: var(--vscode-sideBar-background);
}

.monaco-list .monaco-scrollable-element .monaco-tree-sticky-container .monaco-tree-sticky-row.monaco-list-row {
  position: absolute;
  width: 100%;
  opacity: 1 !important;
  overflow: hidden;
  background-color: var(--vscode-sideBar-background);
}

/* 粘性行悬停 */
.monaco-list .monaco-scrollable-element .monaco-tree-sticky-container .monaco-tree-sticky-row:hover {
  background-color: var(--vscode-list-hoverBackground) !important;
  cursor: pointer;
}

/* 空容器隐藏 */
.monaco-list .monaco-scrollable-element .monaco-tree-sticky-container.empty,
.monaco-list .monaco-scrollable-element .monaco-tree-sticky-container.empty .monaco-tree-sticky-container-shadow {
  display: none;
}
```

#### Tree 尺寸速查表

| 属性 | 值 |
|------|-----|
| Twistie 字号 | `10px` |
| Twistie 宽度 | `16px` |
| Twistie 右内边距 | `6px` |
| 缩进线左偏移 | `16px` (有箭头) / `12px` (无箭头) |
| 筛选器最大宽度 | `200px` |
| 筛选器内边距 | `3px` |
| 筛选器圆角 | `4px` |
| 筛选器输入框高度 | `23px` |
| 加载动画时长 | `1.25s` |

### 5.3 Table 表格

基于虚拟滚动的高性能表格组件。

#### TypeScript 接口

```typescript
interface ITableColumn<TRow, TCell> {
  readonly label: string;                    // 列标题
  readonly tooltip?: string;                 // 列提示
  readonly weight: number;                   // 列宽权重
  readonly templateId: string;               // 渲染模板 ID
  
  readonly minimumWidth?: number;            // 最小宽度
  readonly maximumWidth?: number;            // 最大宽度
  readonly onDidChangeWidthConstraints?: Event<void>;  // 宽度约束变化
  
  project(row: TRow): TCell;                 // 从行数据提取单元格数据
}

interface ITableVirtualDelegate<TRow> {
  readonly headerRowHeight: number;          // 表头行高
  getHeight(row: TRow): number;              // 获取行高
}
```

#### 容器样式

```css
.monaco-table {
  display: flex;
  flex-direction: column;
  position: relative;
  height: 100%;
  width: 100%;
  white-space: nowrap;
  overflow: hidden;
}

.monaco-table > .monaco-split-view2 {
  border-bottom: 1px solid transparent;
}

.monaco-table > .monaco-list {
  flex: 1;
}
```

#### 行样式

```css
.monaco-table-tr {
  display: flex;
  height: 100%;
}
```

#### 表头样式

```css
.monaco-table-th {
  width: 100%;
  height: 100%;
  font-weight: bold;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

#### 单元格样式

```css
.monaco-table-th,
.monaco-table-td {
  box-sizing: border-box;
  flex-shrink: 0;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}
```

#### 列分隔线

```css
.monaco-table > .monaco-split-view2 .monaco-sash.vertical::before {
  content: "";
  position: absolute;
  left: calc(var(--vscode-sash-size) / 2);
  width: 0;
  border-left: 1px solid transparent;
}

/* 过渡动画 */
.monaco-enable-motion .monaco-table > .monaco-split-view2,
.monaco-enable-motion .monaco-table > .monaco-split-view2 .monaco-sash.vertical::before {
  transition: border-color 0.2s ease-out;
}
```

#### Table 特性

| 特性 | 说明 |
|------|------|
| 虚拟滚动 | 只渲染可见行 |
| 列调整 | 基于 SplitView 的可调整列宽 |
| 权重布局 | 按 weight 分配列宽 |
| 最小/最大宽度 | 支持列宽约束 |

### 5.4 SplitView 分割视图

用于创建可调整大小的分割面板布局，支持水平和垂直方向。

#### TypeScript 接口

```typescript
interface ISplitViewStyles {
  readonly separatorBorder: Color;  // 分隔边框颜色
}

enum LayoutPriority {
  Normal = 0,  // 正常优先级
  Low = 1,     // 低优先级（最后调整）
  High = 2     // 高优先级（优先调整）
}

// 每个视图需要实现的接口
interface IView<TLayoutContext = undefined> {
  readonly element: HTMLElement;              // DOM 元素
  readonly minimumSize: number;               // 最小尺寸（像素）
  readonly maximumSize: number;               // 最大尺寸（像素或 Infinity）
  readonly priority?: LayoutPriority;         // 布局优先级
  readonly proportionalLayout?: boolean;      // 是否参与比例布局
  readonly snap?: boolean;                    // 是否支持吸附
  readonly onDidChange: Event<number | undefined>;  // 尺寸约束变化事件
  
  layout(size: number, offset: number, context: TLayoutContext | undefined): void;
  setVisible?(visible: boolean): void;
}
```

#### 容器样式

```css
.monaco-split-view2 {
  position: relative;
  width: 100%;
  height: 100%;
}
```

#### Sash 容器

```css
.monaco-split-view2 > .sash-container {
  position: absolute;
  width: 100%;
  height: 100%;
  pointer-events: none;  /* 穿透点击到下方元素 */
}

.monaco-split-view2 > .sash-container > .monaco-sash {
  pointer-events: initial;  /* Sash 本身可点击 */
}
```

#### 滚动容器

```css
.monaco-split-view2 > .monaco-scrollable-element {
  width: 100%;
  height: 100%;
}

.monaco-split-view2 > .monaco-scrollable-element > .split-view-container {
  width: 100%;
  height: 100%;
  white-space: nowrap;
  position: relative;
}
```

#### 视图样式

```css
.monaco-split-view2 > .monaco-scrollable-element > .split-view-container > .split-view-view {
  white-space: initial;
  position: absolute;
}

/* 隐藏不可见视图 */
.monaco-split-view2 > .monaco-scrollable-element > .split-view-container > .split-view-view:not(.visible) {
  display: none;
}

/* 垂直布局：视图宽度 100% */
.monaco-split-view2.vertical > .monaco-scrollable-element > .split-view-container > .split-view-view {
  width: 100%;
}

/* 水平布局：视图高度 100% */
.monaco-split-view2.horizontal > .monaco-scrollable-element > .split-view-container > .split-view-view {
  height: 100%;
}
```

#### 分隔边框

```css
/* 启用分隔边框时 */
.monaco-split-view2.separator-border > .monaco-scrollable-element > .split-view-container > .split-view-view:not(:first-child)::before {
  content: ' ';
  position: absolute;
  top: 0;
  left: 0;
  z-index: 5;
  pointer-events: none;
  background-color: var(--separator-border);
}

/* 水平布局的垂直分隔线 */
.monaco-split-view2.separator-border.horizontal > .monaco-scrollable-element > .split-view-container > .split-view-view:not(:first-child)::before {
  height: 100%;
  width: 1px;
}

/* 垂直布局的水平分隔线 */
.monaco-split-view2.separator-border.vertical > .monaco-scrollable-element > .split-view-container > .split-view-view:not(:first-child)::before {
  height: 1px;
  width: 100%;
}
```

#### SplitView 特性表

| 特性 | 说明 |
|------|------|
| 方向 | `horizontal`（水平） / `vertical`（垂直） |
| 优先级布局 | 根据 `LayoutPriority` 决定调整顺序 |
| 比例布局 | 窗口调整时按比例分配空间 |
| 吸附 | 视图可吸附到最小尺寸 |
| 滚动支持 | 内置 `SmoothScrollableElement` |

### 5.5 Grid 网格布局

基于 SplitView 的二维网格布局系统。

#### 基础样式

```css
.monaco-grid-view {
  position: relative;
  overflow: hidden;
  width: 100%;
  height: 100%;
}

.monaco-grid-branch-node {
  width: 100%;
  height: 100%;
}
```

#### TypeScript 接口

```typescript
enum Direction {
  Up = 0,
  Down = 1,
  Left = 2,
  Right = 3
}

interface IView extends IGridViewView {
  readonly preferredWidth?: number;   // 双击 Sash 时的首选宽度
  readonly preferredHeight?: number;  // 双击 Sash 时的首选高度
}

interface GridLeafNode<T extends IView> {
  readonly view: T;
  readonly box: Box;
  readonly cachedVisibleSize: number | undefined;
  readonly maximized: boolean;
}

interface GridBranchNode<T extends IView> {
  readonly children: GridNode<T>[];
  readonly box: Box;
}

type GridNode<T extends IView> = GridLeafNode<T> | GridBranchNode<T>;
```

#### Grid 特性

| 特性 | 说明 |
|------|------|
| 嵌套分割 | 支持递归嵌套的分割布局 |
| 方向切换 | 自动在水平/垂直间切换 |
| 视图最大化 | 支持单视图最大化 |
| 边界 Sash | 支持设置边界拖拽条 |

### 5.6 Scrollbar 滚动条

#### CSS 样式

```css
.monaco-scrollable-element > .visible {
  opacity: 1;
  background: rgba(0, 0, 0, 0);
  transition: opacity 100ms linear;
  z-index: 11;
}

.monaco-scrollable-element > .invisible {
  opacity: 0;
  pointer-events: none;
}

.monaco-scrollable-element > .invisible.fade {
  transition: opacity 800ms linear;
}

/* 阴影效果 */
.monaco-scrollable-element > .shadow.top {
  display: block;
  top: 0;
  left: 3px;
  height: 3px;
  width: 100%;
  box-shadow: var(--vscode-scrollbar-shadow) 0 6px 6px -6px inset;
}

.monaco-scrollable-element > .shadow.left {
  display: block;
  top: 3px;
  left: 0;
  height: 100%;
  width: 3px;
  box-shadow: var(--vscode-scrollbar-shadow) 6px 0 6px -6px inset;
}

/* 滚动条样式 */
.monaco-scrollable-element > .scrollbar {
  background: var(--vscode-scrollbar-background);
}

.monaco-scrollable-element > .scrollbar > .slider {
  background: var(--vscode-scrollbarSlider-background);
}

.monaco-scrollable-element > .scrollbar > .slider:hover {
  background: var(--vscode-scrollbarSlider-hoverBackground);
}

.monaco-scrollable-element > .scrollbar > .slider.active {
  background: var(--vscode-scrollbarSlider-activeBackground);
}
```

---

## 6. 反馈与浮层 (Feedback & Overlays)

### 6.1 Dialog 对话框

#### 接口定义

```typescript
interface IDialogOptions {
  cancelId?: number;               // 取消按钮索引
  detail?: string;                 // 详细描述
  alignment?: DialogContentsAlignment;  // 布局对齐方式
  checkboxLabel?: string;          // 复选框标签
  checkboxChecked?: boolean;       // 复选框初始状态
  type?: 'none' | 'info' | 'error' | 'question' | 'warning' | 'pending';
  extraClasses?: string[];         // 额外 CSS 类
  inputs?: IDialogInputOptions[];  // 输入框配置
  keyEventProcessor?: (event) => void;  // 键盘事件处理
  renderBody?: (container) => void;     // 自定义内容渲染
  renderFooter?: (container) => void;   // 自定义页脚渲染
  icon?: ThemeIcon;                // 自定义图标
  buttonOptions?: Array<{ sublabel?: string }>;
  disableCloseAction?: boolean;    // 禁用关闭操作
  disableCloseButton?: boolean;    // 禁用关闭按钮
  disableDefaultAction?: boolean;  // 禁用默认操作
  
  // 样式
  buttonStyles: IButtonStyles;
  checkboxStyles: ICheckboxStyles;
  inputBoxStyles: IInputBoxStyles;
  dialogStyles: IDialogStyles;
}

interface IDialogStyles {
  dialogForeground: string;
  dialogBackground: string;
  dialogShadow: string;
  dialogBorder: string;
  errorIconForeground: string;
  warningIconForeground: string;
  infoIconForeground: string;
  textLinkForeground: string;
}

enum DialogContentsAlignment {
  Horizontal = 0,  // 水平布局（默认）
  Vertical         // 垂直布局
}
```

#### CSS 样式

```css
/* 模态遮罩 */
.monaco-dialog-modal-block {
  position: fixed;
  height: 100%;
  width: 100%;
  left: 0;
  top: 0;
  z-index: 2575;
  display: flex;
  justify-content: center;
  align-items: center;
}

.monaco-dialog-modal-block.dimmed {
  background: rgba(0, 0, 0, 0.3);
}

/* 对话框容器 */
.monaco-dialog-box {
  display: flex;
  flex-direction: column-reverse;
  width: min-content;
  min-width: 500px;
  max-width: 90vw;
  min-height: 75px;
  padding: 10px;
  transform: translate3d(0px, 0px, 0px);
  border-radius: 3px;
}

.monaco-dialog-box.align-vertical {
  min-width: 350px;
}

/* 图标 */
.monaco-dialog-box .dialog-icon.codicon {
  flex: 0 0 48px;
  height: 48px;
  font-size: 48px;
}

/* 消息 */
.monaco-dialog-box .dialog-message {
  line-height: 22px;
  font-size: 18px;
  min-height: 48px;
  margin-bottom: 8px;
}

.monaco-dialog-box .dialog-message-detail {
  line-height: 22px;
}

/* 按钮行 */
.monaco-dialog-box > .dialog-buttons-row {
  display: flex;
  white-space: nowrap;
  padding: 20px 10px 10px;
}

.monaco-dialog-box > .dialog-buttons-row > .dialog-buttons > .monaco-button {
  padding: 4px 10px;
  margin: 4px 5px;
  outline-offset: 2px !important;
}
```

#### 对话框类型图标

| 类型 | 图标 | 颜色变量 |
|------|------|----------|
| `info` | `$(info)` | `--vscode-infoIconForeground` |
| `warning` | `$(warning)` | `--vscode-warningIconForeground` |
| `error` | `$(error)` | `--vscode-errorIconForeground` |
| `question` | `$(question)` | - |
| `pending` | 加载动画 | - |

### 6.2 Hover 悬停提示

用于显示额外信息的悬浮提示组件。

#### CSS 变量

```css
:root {
  --vscode-hover-whiteSpace: normal;           /* 文字换行方式 */
  --vscode-hover-maxWidth: 500px;              /* 最大宽度 */
  --vscode-hover-sourceWhiteSpace: pre-wrap;   /* 代码块换行 */
}
```

#### 基础样式

```css
.monaco-hover {
  cursor: default;
  position: absolute;
  overflow: hidden;
  
  /* 文字选择 */
  user-select: text;
  -webkit-user-select: text;
  
  box-sizing: border-box;
  line-height: 1.5em;
  white-space: var(--vscode-hover-whiteSpace, normal);
}

/* 淡入动画 */
.monaco-hover.fade-in {
  animation: fadein 100ms linear;
}

@keyframes fadein {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

#### 内容样式

```css
/* 内容区域 */
.monaco-hover .hover-contents:not(.html-hover-contents) {
  padding: 4px 8px;
}

/* Markdown 内容最大宽度 */
.monaco-hover .markdown-hover > .hover-contents:not(.code-hover-contents) {
  max-width: var(--vscode-hover-maxWidth, 500px);
  word-wrap: break-word;
}

/* 文字间距 */
.monaco-hover p,
.monaco-hover .code,
.monaco-hover ul,
.monaco-hover h1,
.monaco-hover h2,
.monaco-hover h3,
.monaco-hover h4,
.monaco-hover h5,
.monaco-hover h6 {
  margin: 8px 0;
}

/* 标题行高 */
.monaco-hover h1,
.monaco-hover h2,
.monaco-hover h3,
.monaco-hover h4,
.monaco-hover h5,
.monaco-hover h6 {
  line-height: 1.1;
}

/* 首尾元素去除多余边距 */
.monaco-hover p:first-child,
.monaco-hover .code:first-child,
.monaco-hover ul:first-child {
  margin-top: 0;
}

.monaco-hover p:last-child,
.monaco-hover .code:last-child,
.monaco-hover ul:last-child {
  margin-bottom: 0;
}
```

#### 列表样式

```css
.monaco-hover ul {
  padding-left: 20px;
}

.monaco-hover ol {
  padding-left: 20px;
}

.monaco-hover li > p {
  margin-bottom: 0;
}

.monaco-hover li > ul {
  margin-top: 0;
}
```

#### 代码样式

```css
.monaco-hover code {
  font-family: var(--monaco-monospace-font);
  border-radius: 3px;
  padding: 0 0.4em;
}

.monaco-hover .monaco-tokenized-source {
  white-space: var(--vscode-hover-sourceWhiteSpace, pre-wrap);
}
```

#### 分隔线样式

```css
.monaco-hover hr {
  box-sizing: border-box;
  border-left: 0px;
  border-right: 0px;
  margin-top: 4px;
  margin-bottom: -4px;
  margin-left: -8px;
  margin-right: -8px;
  height: 1px;
}
```

#### 状态栏样式

```css
.monaco-hover .hover-row.status-bar {
  font-size: 12px;
  line-height: 22px;
}

.monaco-hover .hover-row.status-bar .info {
  font-style: italic;
  padding: 0px 8px;
}

.monaco-hover .hover-row.status-bar .actions {
  display: flex;
  padding: 0px 8px;
  width: 100%;
}

.monaco-hover .hover-row.status-bar .actions .action-container {
  margin-right: 16px;
  cursor: pointer;
  overflow: hidden;
  text-wrap: nowrap;
  text-overflow: ellipsis;
}

.monaco-hover .hover-row.status-bar .actions .action-container a {
  color: var(--vscode-textLink-foreground);
  text-decoration: var(--text-link-decoration);
}
```

#### 链接样式

```css
.monaco-hover .hover-contents a.code-link > span {
  text-decoration: underline;
  border-bottom: 1px solid transparent;
  text-underline-position: under;
  color: var(--vscode-textLink-foreground);
}

.monaco-hover .hover-contents a.code-link > span:hover {
  color: var(--vscode-textLink-activeForeground);
}
```

#### Hover 尺寸速查表

| 属性 | 值 |
|------|-----|
| 内容内边距 | `4px 8px` |
| 最大宽度 | `500px` |
| 行高 | `1.5em` |
| 标题行高 | `1.1` |
| 代码圆角 | `3px` |
| 代码内边距 | `0 0.4em` |
| 状态栏字号 | `12px` |
| 状态栏行高 | `22px` |
| 淡入动画 | `100ms linear` |

### 6.3 ProgressBar 进度条

#### 接口定义

```typescript
interface IProgressBarOptions {
  progressBarBackground: string;   // 进度条颜色
}
```

#### 默认样式

```typescript
const unthemedProgressBarOptions = {
  progressBarBackground: undefined  // 默认 #0E70C0
};
```

#### CSS 样式

```css
.monaco-progress-container {
  width: 100%;
  height: 2px;
  overflow: hidden;
}

.monaco-progress-container .progress-bit {
  width: 2%;
  height: 2px;
  position: absolute;
  left: 0;
  display: none;
  background-color: #0E70C0;  /* 默认颜色 */
}

.monaco-progress-container.active .progress-bit {
  display: inherit;
}

/* 确定进度 */
.monaco-progress-container.discrete .progress-bit {
  left: 0;
  transition: width 100ms linear;
}

/* 不确定进度（无限动画） */
.monaco-progress-container.infinite .progress-bit {
  animation-name: progress;
  animation-duration: 4s;
  animation-iteration-count: infinite;
  animation-timing-function: linear;
}

/* 长时间运行优化（降低 GPU 消耗） */
.monaco-progress-container.infinite.infinite-long-running .progress-bit {
  animation-timing-function: steps(100);
}

@keyframes progress {
  from { transform: translateX(0%) scaleX(1) }
  50% { transform: translateX(2500%) scaleX(3) }
  to { transform: translateX(4900%) scaleX(1) }
}
```

#### 方法

| 方法 | 说明 |
|------|------|
| `infinite()` | 开始无限进度动画 |
| `total(value)` | 设置总进度值 |
| `worked(value)` | 增加已完成进度 |
| `setWorked(value)` | 设置已完成进度（0-100） |
| `done()` | 完成（带淡出动画） |
| `stop()` | 立即停止 |
| `show(delay?)` | 显示（可延迟） |
| `hide()` | 隐藏 |

---

## 7. 附录：颜色注册表 (Appendix)

### 7.1 完整颜色系统参考

VS Code 使用语义化的颜色变量系统，支持四种主题模式：`dark`（深色）、`light`（浅色）、`hcDark`（高对比度深色）、`hcLight`（高对比度浅色）。

#### 颜色系统架构

```
colors/
├── baseColors.ts       # 基础颜色（前景、背景、焦点边框等）
├── editorColors.ts     # 编辑器相关颜色
├── inputColors.ts      # 输入控件颜色（按钮、输入框、复选框等）
├── listColors.ts       # 列表/树视图颜色
├── menuColors.ts       # 菜单颜色
├── quickpickColors.ts  # 快速选择器颜色
├── minimapColors.ts    # 小地图颜色
├── miscColors.ts       # 其他颜色（滚动条、进度条、徽章等）
├── searchColors.ts     # 搜索相关颜色
└── chartsColors.ts     # 图表颜色
```

#### 1. 基础颜色 (baseColors)

全局基础颜色变量，作为其他颜色的基准。

```typescript
// 核心前景色
const foreground = {
  dark: '#CCCCCC',
  light: '#616161',
  hcDark: '#FFFFFF',
  hcLight: '#292929'
};

// 禁用状态前景色
const disabledForeground = {
  dark: '#CCCCCC80',    // 50% 透明
  light: '#61616180',
  hcDark: '#A5A5A5',
  hcLight: '#7F7F7F'
};

// 错误前景色
const errorForeground = {
  dark: '#F48771',
  light: '#A1260D',
  hcDark: '#F48771',
  hcLight: '#B5200D'
};

// 描述文本前景色（70% 透明度）
const descriptionForeground = {
  light: '#717171',
  dark: 'transparent(foreground, 0.7)',
  hcDark: 'transparent(foreground, 0.7)',
  hcLight: 'transparent(foreground, 0.7)'
};

// 图标前景色
const iconForeground = {
  dark: '#C5C5C5',
  light: '#424242',
  hcDark: '#FFFFFF',
  hcLight: '#292929'
};

// 焦点边框 - 最重要的交互颜色
const focusBorder = {
  dark: '#007FD4',
  light: '#0090F1',
  hcDark: '#F38518',    // 高对比度使用橙色
  hcLight: '#006BBD'
};

// 高对比度额外边框
const contrastBorder = {
  dark: null,
  light: null,
  hcDark: '#6FC3DF',
  hcLight: '#0F4A85'
};

// 选择背景（工作区级别）
const selectionBackground = null;  // 默认无，由编辑器选择覆盖
```

**文本链接颜色**

```typescript
const textLinkForeground = {
  dark: '#3794FF',
  light: '#006AB1',
  hcDark: '#21A6FF',
  hcLight: '#0F4A85'
};

const textLinkActiveForeground = {
  dark: '#3794FF',
  light: '#006AB1',
  hcDark: '#21A6FF',
  hcLight: '#0F4A85'
};

const textSeparatorForeground = {
  dark: '#ffffff2e',    // 18% 白色
  light: '#0000002e',   // 18% 黑色
  hcDark: '#000000',
  hcLight: '#292929'
};
```

**预格式化文本颜色**

```typescript
const textPreformatForeground = {
  dark: '#D7BA7D',      // 暖黄色
  light: '#A31515',     // 深红色
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const textPreformatBackground = {
  dark: '#FFFFFF1A',    // 10% 白色
  light: '#0000001A',   // 10% 黑色
  hcDark: '#FFFFFF',
  hcLight: '#09345f'
};
```

**引用块颜色**

```typescript
const textBlockQuoteBackground = {
  dark: '#222222',
  light: '#f2f2f2',
  hcDark: null,
  hcLight: '#F2F2F2'
};

const textBlockQuoteBorder = {
  dark: '#007acc80',    // 50% 透明的蓝色
  light: '#007acc80',
  hcDark: '#FFFFFF',
  hcLight: '#292929'
};

const textCodeBlockBackground = {
  dark: '#0a0a0a66',    // 40% 透明
  light: '#dcdcdc66',
  hcDark: '#000000',
  hcLight: '#F2F2F2'
};
```

**基础颜色速查表**

| 变量名 | Dark | Light | HC Dark | HC Light |
|--------|------|-------|---------|----------|
| `foreground` | `#CCCCCC` | `#616161` | `#FFFFFF` | `#292929` |
| `disabledForeground` | `#CCCCCC80` | `#61616180` | `#A5A5A5` | `#7F7F7F` |
| `errorForeground` | `#F48771` | `#A1260D` | `#F48771` | `#B5200D` |
| `icon.foreground` | `#C5C5C5` | `#424242` | `#FFFFFF` | `#292929` |
| `focusBorder` | `#007FD4` | `#0090F1` | `#F38518` | `#006BBD` |
| `contrastBorder` | - | - | `#6FC3DF` | `#0F4A85` |
| `textLink.foreground` | `#3794FF` | `#006AB1` | `#21A6FF` | `#0F4A85` |

#### 2. 编辑器颜色 (editorColors)

编辑器区域的所有颜色定义。

**编辑器基础**

```typescript
const editorBackground = {
  dark: '#1E1E1E',
  light: '#FFFFFF',
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const editorForeground = {
  dark: '#BBBBBB',
  light: '#333333',
  hcDark: '#FFFFFF',
  hcLight: foreground
};
```

**编辑器 Widget**

```typescript
const editorWidgetBackground = {
  dark: '#252526',
  light: '#F3F3F3',
  hcDark: '#0C141F',
  hcLight: '#FFFFFF'
};

const editorWidgetForeground = foreground;

const editorWidgetBorder = {
  dark: '#454545',
  light: '#C8C8C8',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};
```

**错误/警告/信息颜色**

```typescript
// 错误
const editorErrorForeground = {
  dark: '#F14C4C',
  light: '#E51400',
  hcDark: '#F48771',
  hcLight: '#B5200D'
};

const editorErrorBorder = {
  dark: null,
  light: null,
  hcDark: 'rgba(228, 119, 119, 0.8)',
  hcLight: '#B5200D'
};

// 警告
const editorWarningForeground = {
  dark: '#CCA700',
  light: '#BF8803',
  hcDark: '#FFD370',
  hcLight: '#895503'
};

const editorWarningBorder = {
  dark: null,
  light: null,
  hcDark: 'rgba(255, 204, 0, 0.8)',
  hcLight: 'rgba(255, 204, 0, 0.8)'
};

// 信息
const editorInfoForeground = {
  dark: '#59a4f9',
  light: '#0063d3',
  hcDark: '#59a4f9',
  hcLight: '#0063d3'
};

// 提示
const editorHintForeground = {
  dark: 'rgba(238, 238, 238, 0.7)',
  light: '#6c6c6c',
  hcDark: null,
  hcLight: null
};
```

**编辑器选择颜色**

```typescript
const editorSelectionBackground = {
  dark: '#264F78',
  light: '#ADD6FF',
  hcDark: '#f3f518',     // 高对比度使用黄色
  hcLight: '#0F4A85'
};

const editorSelectionForeground = {
  dark: null,
  light: null,
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const editorInactiveSelection = {
  // 50% 透明的 editorSelectionBackground
  dark: 'transparent(editorSelectionBackground, 0.5)',
  light: 'transparent(editorSelectionBackground, 0.5)',
  hcDark: 'transparent(editorSelectionBackground, 0.7)',
  hcLight: 'transparent(editorSelectionBackground, 0.5)'
};
```

**编辑器查找颜色**

```typescript
const editorFindMatch = {
  dark: '#515C6A',
  light: '#A8AC94',
  hcDark: null,
  hcLight: null
};

const editorFindMatchHighlight = {
  dark: '#EA5C0055',     // 33% 透明橙色
  light: '#EA5C0055',
  hcDark: null,
  hcLight: null
};

const editorFindRangeHighlight = {
  dark: '#3a3d4166',     // 40% 透明
  light: '#b4b4b44d',    // 30% 透明
  hcDark: null,
  hcLight: null
};
```

**编辑器悬停颜色**

```typescript
const editorHoverHighlight = {
  dark: '#264f7840',     // 25% 透明
  light: '#ADD6FF26',    // 15% 透明
  hcDark: '#ADD6FF26',
  hcLight: null
};

const editorHoverBackground = editorWidgetBackground;
const editorHoverForeground = editorWidgetForeground;
const editorHoverBorder = editorWidgetBorder;

const editorHoverStatusBarBackground = {
  dark: 'lighten(editorHoverBackground, 0.2)',
  light: 'darken(editorHoverBackground, 0.05)',
  hcDark: editorWidgetBackground,
  hcLight: editorWidgetBackground
};
```

**Inlay Hint 颜色**

```typescript
const editorInlayHintForeground = {
  dark: '#969696',
  light: '#969696',
  hcDark: '#FFFFFF',
  hcLight: '#000000'
};

const editorInlayHintBackground = {
  dark: 'transparent(badgeBackground, 0.10)',
  light: 'transparent(badgeBackground, 0.10)',
  hcDark: 'transparent(#FFFFFF, 0.10)',
  hcLight: 'transparent(badgeBackground, 0.10)'
};
```

**灯泡颜色**

```typescript
const editorLightBulbForeground = {
  dark: '#FFCC00',
  light: '#DDB100',
  hcDark: '#FFCC00',
  hcLight: '#007ACC'
};

const editorLightBulbAutoFixForeground = {
  dark: '#75BEFF',
  light: '#007ACC',
  hcDark: '#75BEFF',
  hcLight: '#007ACC'
};
```

**Diff 编辑器颜色**

```typescript
const diffInserted = {
  dark: '#9ccc2c33',     // 20% 透明绿色
  light: '#9ccc2c40',    // 25% 透明绿色
  hcDark: null,
  hcLight: null
};

const diffRemoved = {
  dark: '#ff000033',     // 20% 透明红色
  light: '#ff000033',
  hcDark: null,
  hcLight: null
};

const diffInsertedOutline = {
  dark: null,
  light: null,
  hcDark: '#33ff2eff',
  hcLight: '#374E06'
};

const diffRemovedOutline = {
  dark: null,
  light: null,
  hcDark: '#FF008F',
  hcLight: '#AD0707'
};

const diffDiagonalFill = {
  dark: '#cccccc33',
  light: '#22222233',
  hcDark: null,
  hcLight: null
};
```

**Widget 颜色**

```typescript
const widgetShadow = {
  dark: 'transparent(#000000, 0.36)',
  light: 'transparent(#000000, 0.16)',
  hcDark: null,
  hcLight: null
};

const widgetBorder = {
  dark: null,
  light: null,
  hcDark: contrastBorder,
  hcLight: contrastBorder
};
```

**Toolbar 颜色**

```typescript
const toolbarHoverBackground = {
  dark: '#5a5d5e50',     // 31% 透明
  light: '#b8b8b850',
  hcDark: null,
  hcLight: null
};

const toolbarHoverOutline = {
  dark: null,
  light: null,
  hcDark: activeContrastBorder,
  hcLight: activeContrastBorder
};

const toolbarActiveBackground = {
  dark: 'lighten(toolbarHoverBackground, 0.1)',
  light: 'darken(toolbarHoverBackground, 0.1)',
  hcDark: null,
  hcLight: null
};
```

**Breadcrumb 颜色**

```typescript
const breadcrumbsForeground = 'transparent(foreground, 0.8)';

const breadcrumbsBackground = editorBackground;

const breadcrumbsFocusForeground = {
  dark: 'lighten(foreground, 0.1)',
  light: 'darken(foreground, 0.2)',
  hcDark: 'lighten(foreground, 0.1)',
  hcLight: 'lighten(foreground, 0.1)'
};

const breadcrumbsPickerBackground = editorWidgetBackground;
```

**合并冲突颜色**

```typescript
const currentBaseColor = 'rgba(64, 200, 174, 0.5)';   // 青绿色
const incomingBaseColor = 'rgba(64, 166, 255, 0.5)';  // 蓝色
const commonBaseColor = 'rgba(96, 96, 96, 0.4)';      // 灰色

const mergeCurrentHeaderBackground = {
  dark: currentBaseColor,
  light: currentBaseColor,
  hcDark: null,
  hcLight: null
};

const mergeIncomingHeaderBackground = {
  dark: incomingBaseColor,
  light: incomingBaseColor,
  hcDark: null,
  hcLight: null
};

const mergeBorder = {
  dark: null,
  light: null,
  hcDark: '#C3DF6F',
  hcLight: '#007ACC'
};
```

**Problems 图标颜色**

```typescript
const problemsErrorIconForeground = editorErrorForeground;
const problemsWarningIconForeground = editorWarningForeground;
const problemsInfoIconForeground = editorInfoForeground;
```

**编辑器颜色速查表**

| 变量名 | Dark | Light | 描述 |
|--------|------|-------|------|
| `editor.background` | `#1E1E1E` | `#FFFFFF` | 编辑器背景 |
| `editor.foreground` | `#BBBBBB` | `#333333` | 编辑器前景 |
| `editorWidget.background` | `#252526` | `#F3F3F3` | 小部件背景 |
| `editorWidget.border` | `#454545` | `#C8C8C8` | 小部件边框 |
| `editorError.foreground` | `#F14C4C` | `#E51400` | 错误波浪线 |
| `editorWarning.foreground` | `#CCA700` | `#BF8803` | 警告波浪线 |
| `editorInfo.foreground` | `#59a4f9` | `#0063d3` | 信息波浪线 |
| `editor.selectionBackground` | `#264F78` | `#ADD6FF` | 选择背景 |
| `editor.findMatchBackground` | `#515C6A` | `#A8AC94` | 当前匹配 |
| `editor.findMatchHighlightBackground` | `#EA5C0055` | `#EA5C0055` | 其他匹配 |
| `editorLightBulb.foreground` | `#FFCC00` | `#DDB100` | 灯泡图标 |
| `diffEditor.insertedTextBackground` | `#9ccc2c33` | `#9ccc2c40` | 插入文本 |
| `diffEditor.removedTextBackground` | `#ff000033` | `#ff000033` | 删除文本 |

#### 3. 输入控件颜色 (inputColors)

所有表单控件的颜色定义。

**输入框**

```typescript
const inputBackground = {
  dark: '#3C3C3C',
  light: '#FFFFFF',
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const inputForeground = foreground;

const inputBorder = {
  dark: null,
  light: null,
  hcDark: contrastBorder,
  hcLight: contrastBorder
};

const inputPlaceholderForeground = {
  dark: 'transparent(foreground, 0.5)',
  light: 'transparent(foreground, 0.5)',
  hcDark: 'transparent(foreground, 0.7)',
  hcLight: 'transparent(foreground, 0.7)'
};
```

**输入选项（如搜索中的匹配大小写按钮）**

```typescript
const inputActiveOptionBorder = {
  dark: '#007ACC',
  light: '#007ACC',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};

const inputActiveOptionBackground = {
  dark: 'transparent(focusBorder, 0.4)',
  light: 'transparent(focusBorder, 0.2)',
  hcDark: 'transparent',
  hcLight: 'transparent'
};

const inputActiveOptionForeground = {
  dark: '#FFFFFF',
  light: '#000000',
  hcDark: foreground,
  hcLight: foreground
};

const inputActiveOptionHoverBackground = {
  dark: '#5a5d5e80',
  light: '#b8b8b850',
  hcDark: null,
  hcLight: null
};
```

**输入验证颜色**

```typescript
// Info
const inputValidationInfoBackground = {
  dark: '#063B49',
  light: '#D6ECF2',
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const inputValidationInfoBorder = {
  dark: '#007acc',
  light: '#007acc',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};

// Warning
const inputValidationWarningBackground = {
  dark: '#352A05',
  light: '#F6F5D2',
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const inputValidationWarningBorder = {
  dark: '#B89500',
  light: '#B89500',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};

// Error
const inputValidationErrorBackground = {
  dark: '#5A1D1D',
  light: '#F2DEDE',
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const inputValidationErrorBorder = {
  dark: '#BE1100',
  light: '#BE1100',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};
```

**下拉选择框**

```typescript
const selectBackground = {
  dark: '#3C3C3C',
  light: '#FFFFFF',
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const selectForeground = {
  dark: '#F0F0F0',
  light: foreground,
  hcDark: '#FFFFFF',
  hcLight: foreground
};

const selectBorder = {
  dark: selectBackground,
  light: '#CECECE',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};

const selectListBackground = {
  dark: null,
  light: null,
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};
```

**按钮**

```typescript
// 主按钮
const buttonForeground = '#FFFFFF';

const buttonBackground = {
  dark: '#0E639C',
  light: '#007ACC',
  hcDark: '#000000',
  hcLight: '#0F4A85'
};

const buttonHoverBackground = {
  dark: 'lighten(buttonBackground, 0.2)',
  light: 'darken(buttonBackground, 0.2)',
  hcDark: buttonBackground,
  hcLight: buttonBackground
};

const buttonBorder = contrastBorder;

const buttonSeparator = 'transparent(buttonForeground, 0.4)';

// 次级按钮
const buttonSecondaryForeground = {
  dark: '#FFFFFF',
  light: '#FFFFFF',
  hcDark: '#FFFFFF',
  hcLight: foreground
};

const buttonSecondaryBackground = {
  dark: '#3A3D41',
  light: '#5F6A79',
  hcDark: null,
  hcLight: '#FFFFFF'
};

const buttonSecondaryHoverBackground = {
  dark: 'lighten(buttonSecondaryBackground, 0.2)',
  light: 'darken(buttonSecondaryBackground, 0.2)',
  hcDark: null,
  hcLight: null
};
```

**Radio 单选按钮**

```typescript
const radioActiveForeground = inputActiveOptionForeground;
const radioActiveBackground = inputActiveOptionBackground;
const radioActiveBorder = inputActiveOptionBorder;

const radioInactiveForeground = null;
const radioInactiveBackground = null;

const radioInactiveBorder = {
  dark: 'transparent(radioActiveForeground, 0.2)',
  light: 'transparent(radioActiveForeground, 0.2)',
  hcDark: 'transparent(radioActiveForeground, 0.4)',
  hcLight: 'transparent(radioActiveForeground, 0.2)'
};

const radioInactiveHoverBackground = inputActiveOptionHoverBackground;
```

**Checkbox 复选框**

```typescript
const checkboxBackground = selectBackground;
const checkboxForeground = selectForeground;
const checkboxBorder = selectBorder;

const checkboxSelectBackground = editorWidgetBackground;
const checkboxSelectBorder = iconForeground;

// 禁用状态 - 使用混合色
const checkboxDisabledBackground = 'mix(checkboxBackground, checkboxForeground, 0.33)';
const checkboxDisabledForeground = 'mix(checkboxForeground, checkboxBackground, 0.33)';
```

**快捷键标签**

```typescript
const keybindingLabelBackground = {
  dark: 'rgba(128, 128, 128, 0.17)',
  light: 'rgba(221, 221, 221, 0.4)',
  hcDark: 'transparent',
  hcLight: 'transparent'
};

const keybindingLabelForeground = {
  dark: '#CCCCCC',
  light: '#555555',
  hcDark: '#FFFFFF',
  hcLight: foreground
};

const keybindingLabelBorder = {
  dark: 'rgba(51, 51, 51, 0.6)',
  light: 'rgba(204, 204, 204, 0.4)',
  hcDark: 'rgba(111, 195, 223, 1)',
  hcLight: contrastBorder
};

const keybindingLabelBottomBorder = {
  dark: 'rgba(68, 68, 68, 0.6)',
  light: 'rgba(187, 187, 187, 0.4)',
  hcDark: 'rgba(111, 195, 223, 1)',
  hcLight: foreground
};
```

**输入控件颜色速查表**

| 变量名 | Dark | Light | 描述 |
|--------|------|-------|------|
| `input.background` | `#3C3C3C` | `#FFFFFF` | 输入框背景 |
| `input.border` | - | - | 输入框边框 |
| `input.placeholderForeground` | 50% 透明 | 50% 透明 | 占位符 |
| `inputOption.activeBorder` | `#007ACC` | `#007ACC` | 激活选项边框 |
| `inputValidation.infoBorder` | `#007acc` | `#007acc` | 信息验证 |
| `inputValidation.warningBorder` | `#B89500` | `#B89500` | 警告验证 |
| `inputValidation.errorBorder` | `#BE1100` | `#BE1100` | 错误验证 |
| `button.background` | `#0E639C` | `#007ACC` | 按钮背景 |
| `button.secondaryBackground` | `#3A3D41` | `#5F6A79` | 次级按钮 |
| `dropdown.background` | `#3C3C3C` | `#FFFFFF` | 下拉框背景 |
| `dropdown.border` | `#3C3C3C` | `#CECECE` | 下拉框边框 |

#### 4. 列表/树视图颜色 (listColors)

列表和树视图的所有状态颜色。

**焦点状态**

```typescript
const listFocusBackground = null;
const listFocusForeground = null;

const listFocusOutline = {
  dark: focusBorder,
  light: focusBorder,
  hcDark: activeContrastBorder,
  hcLight: activeContrastBorder
};

const listFocusAndSelectionOutline = null;  // 焦点+选中状态
```

**选中状态（活跃）**

```typescript
const listActiveSelectionBackground = {
  dark: '#04395E',
  light: '#0060C0',
  hcDark: null,
  hcLight: 'transparent(#0F4A85, 0.1)'
};

const listActiveSelectionForeground = {
  dark: '#FFFFFF',
  light: '#FFFFFF',
  hcDark: null,
  hcLight: null
};

const listActiveSelectionIconForeground = null;
```

**选中状态（非活跃）**

```typescript
const listInactiveSelectionBackground = {
  dark: '#37373D',
  light: '#E4E6F1',
  hcDark: null,
  hcLight: 'transparent(#0F4A85, 0.1)'
};

const listInactiveSelectionForeground = null;
const listInactiveSelectionIconForeground = null;
```

**悬停状态**

```typescript
const listHoverBackground = {
  dark: '#2A2D2E',
  light: '#F0F0F0',
  hcDark: 'transparent(#FFFFFF, 0.1)',
  hcLight: 'transparent(#0F4A85, 0.1)'
};

const listHoverForeground = null;
```

**拖放状态**

```typescript
const listDropOverBackground = {
  dark: '#062F4A',
  light: '#D6EBFF',
  hcDark: null,
  hcLight: null
};

const listDropBetweenBackground = {
  dark: iconForeground,
  light: iconForeground,
  hcDark: null,
  hcLight: null
};
```

**高亮/匹配状态**

```typescript
const listHighlightForeground = {
  dark: '#2AAAFF',
  light: '#0066BF',
  hcDark: focusBorder,
  hcLight: focusBorder
};

const listFocusHighlightForeground = {
  dark: listHighlightForeground,
  light: '#BBE7FF',  // 当有 listActiveSelectionBackground 时
  hcDark: listHighlightForeground,
  hcLight: listHighlightForeground
};
```

**特殊状态**

```typescript
const listInvalidItemForeground = {
  dark: '#B89500',
  light: '#B89500',
  hcDark: '#B89500',
  hcLight: '#B5200D'
};

const listErrorForeground = {
  dark: '#F88070',
  light: '#B01011',
  hcDark: null,
  hcLight: null
};

const listWarningForeground = {
  dark: '#CCA700',
  light: '#855F00',
  hcDark: null,
  hcLight: null
};

const listDeemphasizedForeground = {
  dark: '#8C8C8C',
  light: '#8E8E90',
  hcDark: '#A7A8A9',
  hcLight: '#666666'
};
```

**列表筛选器 Widget**

```typescript
const listFilterWidgetBackground = {
  dark: 'lighten(editorWidgetBackground, 0)',
  light: 'darken(editorWidgetBackground, 0)',
  hcDark: editorWidgetBackground,
  hcLight: editorWidgetBackground
};

const listFilterWidgetOutline = {
  dark: 'transparent',
  light: 'transparent',
  hcDark: '#f38518',
  hcLight: '#007ACC'
};

const listFilterWidgetNoMatchesOutline = {
  dark: '#BE1100',
  light: '#BE1100',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};
```

**Tree 特定颜色**

```typescript
const treeIndentGuidesStroke = {
  dark: '#585858',
  light: '#a9a9a9',
  hcDark: '#a9a9a9',
  hcLight: '#a5a5a5'
};

const treeInactiveIndentGuidesStroke = 'transparent(treeIndentGuidesStroke, 0.4)';
```

**表格颜色**

```typescript
const tableColumnsBorder = {
  dark: '#CCCCCC20',    // 12% 透明
  light: '#61616120',
  hcDark: null,
  hcLight: null
};

const tableOddRowsBackgroundColor = {
  dark: 'transparent(foreground, 0.04)',
  light: 'transparent(foreground, 0.04)',
  hcDark: null,
  hcLight: null
};
```

**Action List 颜色**

```typescript
const editorActionListBackground = editorWidgetBackground;
const editorActionListForeground = editorWidgetForeground;
const editorActionListFocusForeground = listActiveSelectionForeground;
const editorActionListFocusBackground = listActiveSelectionBackground;
```

**列表颜色速查表**

| 变量名 | Dark | Light | 描述 |
|--------|------|-------|------|
| `list.activeSelectionBackground` | `#04395E` | `#0060C0` | 活跃选中背景 |
| `list.activeSelectionForeground` | `#FFFFFF` | `#FFFFFF` | 活跃选中前景 |
| `list.inactiveSelectionBackground` | `#37373D` | `#E4E6F1` | 非活跃选中 |
| `list.hoverBackground` | `#2A2D2E` | `#F0F0F0` | 悬停背景 |
| `list.dropBackground` | `#062F4A` | `#D6EBFF` | 拖放背景 |
| `list.highlightForeground` | `#2AAAFF` | `#0066BF` | 高亮前景 |
| `list.errorForeground` | `#F88070` | `#B01011` | 错误项 |
| `list.warningForeground` | `#CCA700` | `#855F00` | 警告项 |
| `tree.indentGuidesStroke` | `#585858` | `#a9a9a9` | 缩进线 |

#### 5. 菜单颜色 (menuColors)

```typescript
const menuBorder = {
  dark: null,
  light: null,
  hcDark: contrastBorder,
  hcLight: contrastBorder
};

const menuForeground = selectForeground;
const menuBackground = selectBackground;

const menuSelectionForeground = listActiveSelectionForeground;
const menuSelectionBackground = listActiveSelectionBackground;

const menuSelectionBorder = {
  dark: null,
  light: null,
  hcDark: activeContrastBorder,
  hcLight: activeContrastBorder
};

const menuSeparatorBackground = {
  dark: '#606060',
  light: '#D4D4D4',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};
```

#### 6. 快速选择器颜色 (quickpickColors)

命令面板、快速打开等的颜色。

```typescript
const quickInputBackground = editorWidgetBackground;
const quickInputForeground = editorWidgetForeground;

const quickInputTitleBackground = {
  dark: 'rgba(255, 255, 255, 0.105)',
  light: 'rgba(0, 0, 0, 0.06)',
  hcDark: '#000000',
  hcLight: '#FFFFFF'
};

const pickerGroupForeground = {
  dark: '#3794FF',
  light: '#0066BF',
  hcDark: '#FFFFFF',
  hcLight: '#0F4A85'
};

const pickerGroupBorder = {
  dark: '#3F3F46',
  light: '#CCCEDB',
  hcDark: '#FFFFFF',
  hcLight: '#0F4A85'
};

const quickInputListFocusForeground = listActiveSelectionForeground;
const quickInputListFocusBackground = listActiveSelectionBackground;
const quickInputListFocusIconForeground = listActiveSelectionIconForeground;
```

**快速选择器颜色速查表**

| 变量名 | Dark | Light | 描述 |
|--------|------|-------|------|
| `quickInput.background` | `#252526` | `#F3F3F3` | 背景 |
| `quickInputTitle.background` | 10% 白 | 6% 黑 | 标题栏背景 |
| `pickerGroup.foreground` | `#3794FF` | `#0066BF` | 分组标签 |
| `pickerGroup.border` | `#3F3F46` | `#CCCEDB` | 分组边框 |

#### 7. 杂项颜色 (miscColors)

**Sash 拖拽条**

```typescript
const sashHoverBorder = focusBorder;
```

**徽章 Badge**

```typescript
const badgeBackground = {
  dark: '#4D4D4D',
  light: '#C4C4C4',
  hcDark: '#000000',
  hcLight: '#0F4A85'
};

const badgeForeground = {
  dark: '#FFFFFF',
  light: '#333333',
  hcDark: '#FFFFFF',
  hcLight: '#FFFFFF'
};

// 活动栏警告徽章
const activityWarningBadgeForeground = '#FFFFFF';
const activityWarningBadgeBackground = {
  dark: '#B27C00',
  light: '#B27C00',
  hcDark: null,
  hcLight: '#B27C00'
};

// 活动栏错误徽章
const activityErrorBadgeForeground = {
  dark: 'lighten(#000000, 0.2)',
  light: '#FFFFFF',
  hcDark: null,
  hcLight: 'lighten(#000000, 0.2)'
};

const activityErrorBadgeBackground = {
  dark: '#F14C4C',
  light: '#E51400',
  hcDark: null,
  hcLight: '#F14C4C'
};
```

**滚动条**

```typescript
const scrollbarShadow = {
  dark: '#000000',
  light: '#DDDDDD',
  hcDark: null,
  hcLight: null
};

const scrollbarSliderBackground = {
  dark: 'transparent(#797979, 0.4)',
  light: 'transparent(#646464, 0.4)',
  hcDark: 'transparent(contrastBorder, 0.6)',
  hcLight: 'transparent(contrastBorder, 0.4)'
};

const scrollbarSliderHoverBackground = {
  dark: 'transparent(#646464, 0.7)',
  light: 'transparent(#646464, 0.7)',
  hcDark: 'transparent(contrastBorder, 0.8)',
  hcLight: 'transparent(contrastBorder, 0.8)'
};

const scrollbarSliderActiveBackground = {
  dark: 'transparent(#BFBFBF, 0.4)',
  light: 'transparent(#000000, 0.6)',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};

const scrollbarBackground = null;
```

**进度条**

```typescript
const progressBarBackground = {
  dark: '#0E70C0',
  light: '#0E70C0',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};
```

**图表基础**

```typescript
const chartLine = {
  dark: '#236B8E',
  light: '#236B8E',
  hcDark: '#236B8E',
  hcLight: '#236B8E'
};

const chartAxis = {
  dark: 'transparent(#BFBFBF, 0.4)',
  light: 'transparent(#000000, 0.6)',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};

const chartGuide = {
  dark: 'transparent(#BFBFBF, 0.2)',
  light: 'transparent(#000000, 0.2)',
  hcDark: contrastBorder,
  hcLight: contrastBorder
};
```

#### 8. 小地图颜色 (minimapColors)

```typescript
const minimapFindMatch = {
  dark: '#d18616',
  light: '#d18616',
  hcDark: '#AB5A00',
  hcLight: '#0F4A85'
};

const minimapSelection = {
  dark: '#264F78',
  light: '#ADD6FF',
  hcDark: '#FFFFFF',
  hcLight: '#0F4A85'
};

const minimapError = {
  dark: 'rgba(255, 18, 18, 0.7)',
  light: 'rgba(255, 18, 18, 0.7)',
  hcDark: 'rgba(255, 50, 50, 1)',
  hcLight: '#B5200D'
};

const minimapWarning = editorWarningForeground;
const minimapInfo = editorInfoForeground;

const minimapSliderBackground = 'transparent(scrollbarSliderBackground, 0.5)';
const minimapSliderHoverBackground = 'transparent(scrollbarSliderHoverBackground, 0.5)';
const minimapSliderActiveBackground = 'transparent(scrollbarSliderActiveBackground, 0.5)';
```

#### 9. 搜索颜色 (searchColors)

```typescript
const searchResultsInfoForeground = {
  dark: 'transparent(foreground, 0.65)',
  light: foreground,
  hcDark: foreground,
  hcLight: foreground
};

const searchEditorFindMatch = {
  dark: 'transparent(editorFindMatchHighlight, 0.66)',
  light: 'transparent(editorFindMatchHighlight, 0.66)',
  hcDark: editorFindMatchHighlight,
  hcLight: editorFindMatchHighlight
};

const searchEditorFindMatchBorder = {
  dark: 'transparent(editorFindMatchHighlightBorder, 0.66)',
  light: 'transparent(editorFindMatchHighlightBorder, 0.66)',
  hcDark: editorFindMatchHighlightBorder,
  hcLight: editorFindMatchHighlightBorder
};
```

#### 10. 图表颜色 (chartsColors)

语义化的图表颜色系统。

```typescript
const chartsForeground = foreground;
const chartsLines = 'transparent(foreground, 0.5)';

const chartsRed = editorErrorForeground;
const chartsBlue = editorInfoForeground;
const chartsYellow = editorWarningForeground;
const chartsOrange = minimapFindMatch;

const chartsGreen = {
  dark: '#89D185',
  light: '#388A34',
  hcDark: '#89D185',
  hcLight: '#374e06'
};

const chartsPurple = {
  dark: '#B180D7',
  light: '#652D90',
  hcDark: '#B180D7',
  hcLight: '#652D90'
};
```

**图表颜色速查表**

| 变量名 | Dark | Light | 用途 |
|--------|------|-------|------|
| `charts.foreground` | 继承 | 继承 | 图表前景 |
| `charts.red` | `#F14C4C` | `#E51400` | 错误/负面 |
| `charts.blue` | `#59a4f9` | `#0063d3` | 信息/中性 |
| `charts.yellow` | `#CCA700` | `#BF8803` | 警告 |
| `charts.orange` | `#d18616` | `#d18616` | 高亮 |
| `charts.green` | `#89D185` | `#388A34` | 成功/正面 |
| `charts.purple` | `#B180D7` | `#652D90` | 特殊 |

#### 颜色工具函数

VS Code 使用以下函数来派生颜色：

```typescript
// 透明度
transparent(color, opacity)  // 设置透明度

// 亮度调整
lighten(color, factor)       // 变亮
darken(color, factor)        // 变暗

// 混合
lessProminent(color, background, factor1, factor2)  // 降低突出度

// 条件
ifDefinedThenElse(condition, ifTrue, ifFalse)  // 条件选择

// 备选
oneOf(color1, color2)  // 使用第一个已定义的颜色
```

#### 主题模式对比总结

| 类别 | Dark 特点 | Light 特点 | HC Dark 特点 | HC Light 特点 |
|------|-----------|------------|--------------|---------------|
| **背景** | 深灰 `#1E1E1E` | 纯白 `#FFFFFF` | 纯黑 `#000000` | 纯白 `#FFFFFF` |
| **前景** | 浅灰 `#CCCCCC` | 深灰 `#616161` | 纯白 `#FFFFFF` | 深黑 `#292929` |
| **焦点** | 蓝色 `#007FD4` | 蓝色 `#0090F1` | 橙色 `#F38518` | 深蓝 `#006BBD` |
| **错误** | 浅红 `#F48771` | 深红 `#A1260D` | 浅红 `#F48771` | 深红 `#B5200D` |
| **警告** | 金色 `#CCA700` | 棕色 `#BF8803` | 浅金 `#FFD370` | 深棕 `#895503` |
| **选择** | 深蓝 `#264F78` | 浅蓝 `#ADD6FF` | 黄色 `#f3f518` | 深蓝 `#0F4A85` |
| **边框** | 通常无 | 通常无 | 必须有高对比 | 必须有高对比 |

---

> 📁 源代码位置: `src/vs/base/browser/ui/`  
> 📁 颜色定义: `src/vs/platform/theme/common/colors/`  
> 📄 本文档生成自 VS Code v1.106

---

## 8. Web 项目适配指南 (Web Adaptation Guide)

本节介绍如何将上述 VS Code 组件规范应用到普通的 Web 项目（Vue, React, Angular 或纯 HTML）中。

### 8.1 基础环境配置

#### 1. 引入字体与图标
VS Code 组件严重依赖 Codicons 图标集。在你的项目中，需要安装并引入该图标库。

**安装 Codicons**
```bash
npm install @vscode/codicons
```

**引入 CSS**
```javascript
// 在入口文件 (main.js / main.ts) 中引入
import '@vscode/codicons/dist/codicon.css';
```

**配置字体栈**
在全局 CSS 文件中设置基础字体变量：
```css
:root {
  /* 优先使用 MiSans，回退到系统字体 */
  --vscode-font-family: "MiSans", -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", "Helvetica Neue", Arial, sans-serif;
  --vscode-font-size: 13px;
  --vscode-font-weight: 400;
}

body {
  font-family: var(--vscode-font-family);
  font-size: var(--vscode-font-size);
  color: var(--vscode-foreground);
  background-color: var(--vscode-editor-background);
}
```

#### 2. 全局样式重置 (CSS Reset)
VS Code 组件默认使用 `border-box` 盒模型。

```css
*, *:before, *:after {
  box-sizing: border-box;
}

a {
  text-decoration: none;
}

/* 禁用默认的焦点轮廓，使用 VS Code 风格的 focusBorder */
:focus {
  outline: none;
}
```

### 8.2 兼容性处理

#### 1. 图标语法转换
文档中提到的 `$(icon-name)` 是 VS Code 内部的语法。在 Web 项目中，请使用标准的 HTML 结构：

**VS Code 内部语法:**
```typescript
{ icon: '$(gear)' }
```

**Web 项目写法:**
```html
<i class="codicon codicon-gear"></i>
```

#### 2. 样式变量作用域
VS Code 使用 `.vs` (浅色), `.vs-dark` (深色), `.hc-black` (高对比) 类名来切换主题。
在 Web 项目中，建议将 CSS 变量定义在 `:root` 中，并通过给 `body` 添加类名来切换主题。

```css
/* 默认深色主题变量 */
:root {
  --vscode-foreground: #CCCCCC;
  --vscode-editor-background: #1E1E1E;
  /* ...其他变量 */
}

/* 浅色主题覆盖 */
body.light-theme {
  --vscode-foreground: #616161;
  --vscode-editor-background: #FFFFFF;
  /* ...其他变量 */
}
```

### 8.3 常见问题修复

**Q: 为什么图标显示为方块？**
A: 检查是否正确引入了 `codicon.css`，并确保字体文件 (`codicon.ttf`) 能够被正确加载（配置构建工具的 loader）。

**Q: 组件没有圆角？**
A: 确保定义了 `--radius-xs` 等圆角变量，或直接在 CSS 中硬编码 `border-radius: 2px`。

**Q: TypeScript 报错 `IHoverDelegate` 未找到？**
A: 这些是 VS Code 内部接口。在你的项目中，可以忽略这些类型，或者定义一个简单的替代接口：
```typescript
// 简单的替代接口
interface IHoverDelegate {
  showHover(options: any): void;
}
```
