# 保留/冻结/清理清单（最新版）

更新时间：2026-04-07

## 一、保留（当前稳定可运行）

1. `run_new_gui.py` 默认模式（稳定新界面）
2. `launchers/启动-新界面.bat`
3. `launchers/run_classify.bat`
4. `src/core/*` 与 `src/utils/*`

## 二、冻结（可运行但不作为默认主线）

1. `run_new_gui.py --modular`（`src/gui/app.py`）
2. `run_new_gui.py --traditional`（`src/gui/main_window.py`）

## 三、清理候选（分批处理）

1. GUI 中英文混用文案
2. `native_ui.py` 高 DPI 旧枚举警告
3. `src/gui/legacy/` 物理归档迁移后的路径引用清理
4. 多入口文档冗余描述
