# 项目体检修复 PR 清单（状态更新）

更新时间：2026-04-07

## 已完成

1. 入口与文档一致性第一轮修复。
2. 配置读取一致性修复。
3. 核心路径导入兼容修复。
4. GUI 重复定义与显示异常修复。
5. 最小测试与 CI 门禁可运行。
6. 入口统一落地（`run_new_gui.py` 统一路由 + launcher 收敛）。

## 进行中

1. 模块化入口（`src/gui/app.py`）全流程回归。
2. GUI 文件物理归档迁移方案验证。

## 下一批 PR 建议

### PR-A：模块化 GUI 功能回归

1. 训练流程回归。
2. 分类流程回归。
3. 设置读写回归。

### PR-B：legacy 归档迁移

1. 建立 `src/gui/legacy/`。
2. 迁移 `native_ui.py` 与 `main_window.py`。
3. 修复导入与启动脚本引用。

### PR-C：文档治理收口

1. 清理重复章节与过时说明。
2. 固化文档状态总览。
3. 新增“文档变更同步检查”清单。
