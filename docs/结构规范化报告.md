# 项目结构规范化报告（第三批）

日期：2026-04-07  
范围：入口统一落地、项目结构规划更新、文件整理与文档对齐。

## 1. 本批结论

1. GUI 入口已统一到 `run_new_gui.py` 路由层。
2. 稳定新界面默认入口为 `src/gui/native_ui.py`。
3. 模块化新界面通过 `--modular` 显式进入，不再与稳定入口混名。

## 2. 入口真相表（最新版）

| 用户入口 | 实际目标 | 状态 | 备注 |
|---|---|---|---|
| `run_new_gui.py` | `src/gui/native_ui.py` | 稳定主线 | 默认 |
| `run_new_gui.py --modular` | `src/gui/app.py` | 预览入口 | 待完整回归 |
| `run_new_gui.py --traditional` | `src/gui/main_window.py` | 历史兼容 | 传统界面 |
| `launchers/启动-新界面.bat` | `run_new_gui.py` | 稳定主线 | 已统一 |
| `launchers/启动-新界面-模块化预览.bat` | `run_new_gui.py --modular` | 预览入口 | 新增 |
| `launchers/快速启动-传统界面.bat` | `run_new_gui.py --traditional` | 传统入口 | 已统一 |
| `launchers/run_classify.bat` | `src/cli/classify_cli.py` | 稳定主线 | CLI |

## 3. 已落地改动

1. `run_new_gui.py`：改为统一路由器（stable/modular/traditional）。
2. `launchers/启动-新界面.bat`：改为调用 `run_new_gui.py`。
3. `launchers/快速启动-传统界面.bat`：改为调用 `run_new_gui.py --traditional`。
4. 新增 `launchers/启动-新界面-模块化预览.bat`。
5. `start.bat`：菜单拆分为稳定新界面、模块化预览、传统界面、CLI。

## 4. 结构规划与文件整理

### 4.1 本次整理原则

1. 不做高风险物理搬迁（避免大量导入路径回归风险）。
2. 先完成“入口语义收敛 + 文档真相统一”。
3. 后续再做 `src/gui/legacy/` 物理归档迁移。

### 4.2 规划状态

1. 入口统一：已完成。
2. 文档对齐：已完成。
3. 物理迁移：未开始（下一阶段）。

## 5. 后续阶段

1. 为 `src/gui/app.py` 补训练/分类/设置全流程回归。
2. 回归通过后，推进 `native_ui.py` 与 `main_window.py` 的 legacy 归档迁移。
3. 最终收敛到“一个对外新界面主线 + 两个显式兼容入口”。
