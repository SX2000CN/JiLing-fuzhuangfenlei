# 项目文件结构规划（主线收敛版）

更新时间：2026-04-07

## 1. 现状结构（已存在）

```text
JiLing-fuzhuangfenlei/
├─ src/
│  ├─ core/
│  ├─ cli/
│  ├─ gui/
│  └─ utils/
├─ launchers/
├─ config/
├─ models/
├─ data/
├─ fonts/
├─ logs/
├─ outputs/
├─ tests/
├─ docs/
├─ build/
└─ openspec/
```

## 2. 本次已落地整理

1. 统一 GUI 路由入口：`run_new_gui.py`。
2. 新增模块化预览脚本：`launchers/启动-新界面-模块化预览.bat`。
3. 所有 GUI launcher 通过统一入口路由。
4. 菜单脚本 `start.bat` 已按“稳定/预览/传统/CLI”分流。

## 3. 目标结构（后续阶段）

```text
src/gui/
├─ app.py               # 未来唯一新界面主入口
├─ legacy/
│  ├─ native_ui.py
│  └─ main_window.py
├─ components/
├─ pages/
├─ theme/
└─ workers/
```

说明：当前先完成入口收敛，物理迁移将在模块化全量回归后执行。

## 4. 文件整理策略

1. 先语义收敛（入口与命名统一）。
2. 再功能回归（模块化全流程通过）。
3. 最后物理迁移（legacy 归档）。

## 5. 文档同步规则

入口变化时必须同步更新：

1. `README.md`
2. `docs/结构规范化报告.md`
3. `docs/新版UI使用说明.md`
4. `docs/开发者指南.md`
5. `docs/文档状态总览.md`
