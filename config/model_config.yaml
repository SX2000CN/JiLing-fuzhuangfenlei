# 模型配置文件
model:
  # 支持的模型类型: tf_efficientnetv2_s, convnext_tiny, resnet50, vit_base_patch16_224, swin_tiny_patch4_window7_224
  # 兼容别名: efficientnetv2_s -> tf_efficientnetv2_s
  name: "tf_efficientnetv2_s"
  num_classes: 3
  pretrained: true
  
  # 图像尺寸
  image_size: 580
  
  # 类别标签
  classes:
    - "主图"
    - "细节" 
    - "吊牌"

# 模型路径配置
paths:
  saved_models: "models/saved_models"
  checkpoints: "models/checkpoints"
  exports: "models/exports"

# 模型性能配置
performance:
  # 混合精度训练
  use_amp: true
  
  # 模型编译优化 (PyTorch 2.0+)
  compile_model: true
  
  # 数据加载器设置
  dataloader:
    num_workers: 4
    pin_memory: true
    persistent_workers: true
